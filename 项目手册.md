# Rovio（lc-course）项目手册 · 代码级复习指南

> 写给作者本人的复习手册。按「总览 → 引擎 → 微服务 → 前端 → 部署测试 → 设计精髓与坑」的顺序，从头到尾把项目过一遍。所有行号、函数名、事件名均来自当前代码库实读。

---

# 第一部分 · 项目总览

## 1.1 这是什么

**Rovio** —— 基于 **微服务 + Supervisor-Worker 多智能体编排** 的自动化办公助手。用户一句话触发「规划 → 多 Agent 并行执行 → 反思修订 → 汇总」的完整工作流，集成 RAG 知识库、联网搜索、代码沙箱、网页抓取、Word 文档生成/填充、产物管理、长期记忆，全程 SSE 流式可观测。

架构演进：旧版是 Streamlit + `SupervisorAgent` + `ReactAgent` 两层单体（`app.py`/`fastapi_app/`/`react_agent.py`，已废弃），后演进为单体 FastAPI（库 `agent_records`），最终拆分为现在的 **6 微服务 + AIRAGAgent 引擎包** 形态。`langgraph.json` 是历史保留，不再是运行入口。

## 1.2 架构总图

```
                              ┌──────────────────────────────────────┐
                              │           前端  Vue 3 + Vite          │
                              │  ChatView / KbManager / AdminView ... │
                              └───────────────────┬──────────────────┘
                                                  │  /api/*  (SSE 流式)
                                                  ▼
                                          ┌───────────────┐
                                          │  Nginx / Vite │  按 URL 前缀路由
                                          │     代理       │
                                          └───────┬───────┘
                ┌─────────────────────────┼─────────────────────────────────┐
                ▼                         ▼                                 ▼
        ┌──────────────┐         ┌──────────────────┐               ┌──────────────┐
        │ auth_service │         │  chat_service    │               │  kb_service  │
        │   :8001      │         │     :8003        │               │    :8004     │
        │ JWT/注册/登录 │         │  Orchestrator    │               │ 多知识库 RAG │
        │ 库 lc_auth   │         │  库 lc_chat      │               │ 库 lc_kb     │
        └──────┬───────┘         └────────┬─────────┘               └──────┬───────┘
               │                          │ 复用 AIRAGAgent 引擎           │ 复用 AIRAGAgent 引擎
        ┌──────┴───────┐                  │                                │
        │ user_service │           ┌──────┴─────────┐              ┌───────┴──────┐
        │   :8002      │           │  AIRAGAgent    │              │  ChromaDB    │
        │ 档案/课表/模型│           │  Orchestrator  │              │  kb_store    │
        │ 库 lc_user   │           │  + SubAgents   │              │  向量存储     │
        └──────────────┘           └──────┬─────────┘              └──────────────┘
                                          │ Redis Pub/Sub 事件
                                          ▼
                                  ┌──────────────────┐         ┌──────────────┐
                                  │  admin_service   │         │ file_service │
                                  │     :8005        │         │    :8006     │
                                  │ 统计/审计/限流    │         │ 文件系统即存储│
                                  │ 库 lc_admin      │         │   无 DB      │
                                  └──────────────────┘         └──────────────┘

  ── MySQL 5 库：lc_auth / lc_user / lc_chat / lc_kb / lc_admin ──
  ── Redis：Pub/Sub 事件总线 + 会话缓存 + 限流计数 + 任务队列 ──
```

## 1.3 六个微服务速查表

| 服务 | 端口 | 数据库 | 职责 | 是否依赖 AIRAGAgent |
|------|------|--------|------|---------------------|
| auth_service | 8001 | lc_auth | 注册/登录/JWT/密保找回/设备管理 | 否（自包含） |
| user_service | 8002 | lc_user | 用户档案/头像/课表 Excel/自配模型/注销编排 | 否（自包含） |
| **chat_service** | 8003 | lc_chat | **核心**：`/api/chat` SSE 入口 → Orchestrator | 是（PYTHONPATH 指项目根） |
| kb_service | 8004 | lc_kb | 多知识库 CRUD + 文档索引/检索（ChromaDB） | 是 |
| admin_service | 8005 | lc_admin | 统计看板/用户管理/Redis 事件订阅落库 | 否（自包含） |
| file_service | 8006 | 无 | Word 上传下载 + AI 工作区文件浏览 | 否（自包含） |

## 1.4 技术选型一览

| 层级 | 技术 | 理由 |
|------|------|------|
| 前端 | Vue 3 + Vite，无 Pinia | `useChat` 模块级单例管状态；路由级 `() => import()` 代码分割 |
| Markdown | **自研零依赖渲染器** `utils/markdown.js` | markdown-it/vue-virtual-scroller 在 package.json 里但**实际未使用**（冗余依赖） |
| 后端 | FastAPI + Uvicorn | 原生 async + SSE；Pydantic 校验 |
| 编排 | LangChain `create_agent` + 中间件链 | 注意：**没有真正用 LangGraph 图**，`create_agent` 内部是 LangGraph 实现 |
| LLM | DeepSeek（thinking 模式） | 流式 `reasoning_content` 作为思考过程推送 |
| 向量库 | ChromaDB 嵌入式 | 集合 `kb_store`，按 chunk 元数据 `kb_id/doc_id` 隔离 |
| 嵌入 | OpenAI 兼容端点（百炼）/ DashScope | `kb/embedding.py` 可切换 |
| DB | MySQL 8，每服务独立库 | pymysql 短连接，**无 ORM、无连接池** |
| 缓存/事件 | Redis | Pub/Sub + ZSET 滑动窗口限流 + 缓存 + 任务队列 |
| 鉴权 | JWT HS256，密钥共享 | 各服务独立验签，**不查库不回调 auth** |
| 包管理 | uv + uv.lock | `uv sync` 安装 |

## 1.5 启动方式速查

```powershell
# 1. 配置
Copy-Item deploy\backend\.env.example deploy\backend\.env   # 填 MYSQL_PASSWORD / JWT_SECRET_KEY / DEEPSEEK_API_KEY 等
# 2. 依赖
uv sync
cd frontend; npm install; cd ..
# 3. 一键启动 6 个微服务
.\deploy\backend\start_microservices.bat
# 4. 前端
cd frontend; npm run dev   # http://localhost:5173（内置微服务代理，无需 nginx）
# 5. 验证
foreach ($p in 8001..8006) { Invoke-RestMethod "http://localhost:$p/api/health" }
```

Docker 全栈：`docker compose up -d --build`（migrator 先跑 alembic baseline，再起 6 服务 + 前端 nginx）。

---

# 第二部分 · AIRAGAgent/agent —— 智能体编排核心（最重要）

这是整个项目的心脏。核心范式是 **Planner → Executor → Reflector → Finalizer** 四阶段，由 `Orchestrator` 一个类驱动。

## 2.1 orchestrator.py —— Supervisor 主循环

**职责**：把用户请求拆成 Plan、调度 SubAgent 执行、反思修订、流式汇总答案，全程产出 SSE 事件。

### 关键数据模型（Pydantic）

| 名称 | 定义 | 作用 |
|---|---|---|
| `StepSpec` (L59) | `description / subagent / depends_on` | Planner 产出的单步规约，`depends_on` 是 step_idx 列表（从 0 开始） |
| `PlanSpec` (L73) | `goal / steps` | 完整计划规约 |
| `Reflection` (L79) | `action / feedback` | 反思结果，action ∈ `accept / retry / revise / ask_user` |

### 主入口 execute_stream（L731-822）三段式

```python
def execute_stream(self, query, chat_history, user_id, session_id):
    # 1) 规划
    plan_gen = self._plan_stream(query, chat_history, user_id)
    while True:
        try:
            chunk = next(plan_gen); yield chunk          # 思考过程逐个透传
        except StopIteration as e:
            spec = e.value; break                        # generator 返回值 = PlanSpec
    plan = self._build_plan(spec, user_id, session_id)   # 落库 plans + plan_steps
    yield {"type": "plan_created", ...}

    # 2) 执行 + 反思主循环
    while not plan.is_all_done():
        ready = plan.get_ready_steps()                   # 依赖感知调度
        if len(ready) == 1:
            result = yield from self._execute_step(plan, ready[0], ...)
            reflection = self._reflect(...)
            # accept / retry(≤1) / revise(≤2, continue 重进循环) / ask_user
        else:
            yield from self._execute_steps_parallel(plan, ready, ...)  # 并行批次

    # 3) 汇总
    final_text = yield from self._finalize(plan, query, llm)
    yield {"type": "plan_completed", ...}
```

**`next()` + `StopIteration.value`** 是「generator 既流式又返回值」的经典技巧——`_plan_stream` 一边 yield 思考事件，一边把解析好的 PlanSpec 通过 return 带回来。

### Planner：_plan_stream（L290-339）

- 用 `llm.stream()` 逐 chunk 处理：从 `chunk.additional_kwargs["reasoning_content"]` 取 DeepSeek thinking 推理过程 yield `thinking` 事件；正文累加后 `_extract_json` + `json.loads` + `PlanSpec(**data)`。
- **有意不用 `with_structured_output`**（L255 注释）：DeepSeek thinking 模式不支持 `tool_choice`。这是全项目最重要的兼容性取舍——Planner 输出是**纯文本 JSON**，靠 `_extract_json`（L169）容错提取（去 ``` 围栏、取第一个 `{` 到最后一个 `}`）。
- 规划前 `_recall_relevant_memory`（L231）召回 10 条长期记忆注入上下文，失败静默降级；`_format_history`（L217）只取最近 6 条历史、每条截 200 字。
- Planner 失败兜底：`PlanSpec(goal=query, steps=[StepSpec(subagent="")])`——退化为单步直接回答。

### Executor：_execute_step（L361-462）

```python
token_plan = current_plan_id_var.set(plan.id)        # 三个 artifact ContextVar
token_session = current_session_id_var.set(plan.session_id)
token_step = current_step_idx_var.set(step.step_idx)
# ... finally 里逐个 reset(token)，早退分支也手动 reset，不泄漏
```

- `increment_attempts=bool(feedback)`：带反馈重跑才累加 attempts，首次执行不算。
- 前序结果注入：`task_desc += self._build_step_context(plan, exclude={step.step_idx})`。
- **两条执行路径**：
  1. `subagent` 为空 → Orchestrator LLM 直接回答。**单步 plan 用旧协议事件名 `thinking`/`output`，多步用 `step_thinking`/`step_output`**（前端兼容设计）；单步时用原始 `query` 而非 `step.description` 作 HumanMessage（Planner 改写会偏语义）。
  2. `subagent` 非空 → `SubAgentRunner(sub, llm)` 每次新建实例，`runner.execute_stream` 事件改写成 `step_*` 透传。
- 结果 `[:65000]` 截断写库（匹配 MySQL TEXT 65535 字节上限），`_save_step_result` 落盘 `tasks/<plan_id>/plan_results/step_<idx>.md`，yield `step_completed`。
- **异常不抛出**：单步失败写 FAILED + yield `step_failed` + return 空串，主循环继续跑后续步骤。

### 并行执行：_execute_steps_parallel（L464-514）

- 只有 `all(s.subagent for s in steps)` 才真并行；混有直接回答步则退化串行。
- 并行时**故意不设 step_idx ContextVar**（L472 注释），每个 spec 的 description 注入 `_build_step_context(plan, exclude=batch_indices)`——**排除整个并行批次**，因为批次内步骤彼此独立、不应互相引用结果。
- 并行步**不逐个反思**（太慢），只对失败步不调 LLM 直接重试一次。

### Reflector：_reflect（L589-622）

两条短路：
- subagent 为空 → 直接 accept；
- FAILED 步若 `attempts <= MAX_RETRIES(=1)` → 直接 retry（不调 LLM），否则 accept 防死循环。

正常路径把 `step.result[:2000]` 喂给 Reflector LLM，解析失败默认 accept。约束：`MAX_RETRIES=1`、`MAX_REVISIONS=2`。

### Finalizer：_finalize（L657-725）

- 单步直接回答的 plan 不二次加工（output 已在执行时流式推送过）。
- 全失败 → 直接输出失败话术，plan 置 FAILED。
- 部分/全部成功 → 每步 `result[:1500]` 拼 summary 让 Finalizer LLM 整合，流式 yield；流式失败 fallback 直接拼接各步结果。

### SSE 事件协议全表

| 事件 | 时机 |
|---|---|
| `thinking` | Planner 推理流 / 单步直接回答的思考 |
| `plan_created` | 计划落库后 |
| `step_started` / `step_thinking` / `step_output` / `step_completed` / `step_failed` | 多步执行过程 |
| `plan_reflecting` / `plan_revised` | 反思与修订 |
| `ask_user` | Reflector 要求用户澄清（plan 直接置 COMPLETED 返回） |
| `output` | 单步直接回答正文 / Finalizer 汇总 |
| `plan_completed` | 全部完成 |
| `message_ids` | chat_service 落库后补发（前端用于反馈/分叉） |

## 2.2 plan.py —— Plan 数据模型与持久化

- **状态枚举**：`PlanStatus`（planning/executing/reflecting/completed/failed/revised/cancelled）、`StepStatus`（pending/ready/running/completed/failed/skipped/blocked）。注意 READY 定义了但从不落库（ready 是计算出来的）。
- `plan_id = f"plan_{毫秒时间戳}_{uuid前6位}"`；`depends_on` 在 DB 里存逗号分隔字符串，`from_row` 解析回 int 列表。

### get_ready_steps（L143-158）—— 依赖解析核心

```python
completed_idxs = {s.step_idx for s in self.steps if s.status == COMPLETED}
failed_idxs    = {s.step_idx for s in self.steps if s.status in (FAILED, BLOCKED)}
for s in self.steps:
    if s.status != PENDING: continue
    if all(dep in completed_idxs for dep in s.depends_on):
        ready.append(s)                        # 同批返回多个 = 并行批次
    elif any(dep in failed_idxs for dep in s.depends_on):
        s.status = BLOCKED                     # 有副作用的 getter！
```

**坑**：BLOCKED 只改内存，主循环负责持久化（orchestrator 已配合处理）。

### CRUD 要点

- `create_plan`：`cursor.lastrowid` 回填 `step.id`。
- `update_step_status`：动态拼 SET；running 写 `started_at`，终态写 `finished_at`；`attempts = attempts + 1` 原子自增。
- `replace_plan_steps`：修订时**整体 DELETE 再 INSERT**（不留旧步历史），plan 重置为 executing。
- `get_session_plan`：`ORDER BY (status IN ('planning','executing',...)) DESC, created_at DESC`——布尔表达式排序，优先取未完成 plan。

## 2.3 sub_agent.py —— SubAgent 体系

### SubAgent dataclass（L32-75）

```python
@dataclass
class SubAgent:
    name: str; description: str; tools: list
    system_prompt: str; workflow_hint: str
    max_tool_calls: int = 20
    category: str = "domain"

    def build_agent(self, llm=None):
        return create_agent(
            model=llm or chat_model,
            system_prompt=identity + self.system_prompt,
            tools=self.tools,
            middleware=[ToolCallLimitMiddleware(run_limit=self.max_tool_calls, exit_behavior="end"),
                        skill_aware_monitor, log_before_model, smart_prompt_switch],
        )
```

**每次调用新建独立 agent 实例**——这是线程安全的关键。

### SubAgentRunner.execute_stream（L111-195）

- 与旧 ReactAgent 流式协议完全一致（thinking/thinking_end/output），Orchestrator 可无缝替换。
- `runtime_context["active_skills"] = [sub_agent.name]` 注入，供 `smart_prompt_switch` 中间件识别当前激活的 SubAgent。
- `stream_mode="messages"` + `context=runtime_context`；`except TypeError` 回退不带 context 的调用（兼容旧 langchain 的防御写法）。
- **兜底**（L191-195）：调了工具但没有任何可见 output 时，把所有工具结果拼成列表补发 output——防止 agent 只调工具不总结导致前端空白。

### run_steps_parallel（L271-333）—— 全项目最精妙的并发细节

```python
parent_ctx = contextvars.copy_context()       # 主线程捕获快照（user_id/ip/plan_id...）
def _run_one(spec):
    child_ctx = parent_ctx.copy()             # 每个 worker 复制独立副本！
    child_ctx.run(_inner)                     # 在副本里跑流式消费
```

**为什么不能直接共享 parent_ctx**：`Context` 对象非线程安全，多线程共享同一个 `Context.run()` 会报 "already entered"。每个 worker 必须 `.copy()`。`ThreadPoolExecutor(max_workers=min(5, len(specs)))`，`as_completed` 收集后按 step_idx 排序保证输出稳定；单步异常包装成 `success=False` 不炸批次。

## 2.4 sub_agents/builtin.py —— 11 个内置 SubAgent

`register_all_subagents()` 用 `registry._initialized` 守卫保证幂等（模块加载、`__init__.py`、Orchestrator.__init__ 三处调用）。

| category | name | max_tool_calls | 工作流 |
|---|---|---|---|
| domain | weather | 20 | get_user_location → get_city_code → get_weather |
| domain | schedule | 20 | get_current_month → get_schedule |
| domain | report | 20 | get_user_id → fill_context_for_report → fetch_external_data → create_artifact |
| domain | knowledge | 20 | rag_summarize |
| domain | document | 20 | auto_fill_word |
| domain | search | 20 | search (Tavily) |
| builtin | codexec | **15** | 代码沙箱（装包链路易触顶，故意调低） |
| builtin | browser | **25** | 抓取链路长（搜索页→extract_links→详情页→403 回退 rendered），15 次不够 |
| builtin | wordgen | 12 | run_word_code（WordDoc 排版类） |
| memory | memory | 20 | remember / recall / recall_one / forget |
| artifact | artifact | 20 | create_artifact / list_artifacts / get_artifact |

**Prompt 是驯化精华**：
- `CODEXEC_PROMPT`：三级隔离工作目录、强制相对路径、禁止绝对路径（触发 UAC/Defender）、30 天清理、反爬规范（完整 headers + `sleep(random.uniform(1,3))` + 403 停手转 browser）。
- `WORDGEN_PROMPT`：4 套 theme（modern/official/fresh/academic）、硬性规则"只准用 WordDoc 禁止裸 python-docx"、读前序数据用 `plan_results/step_<idx>.md`。
- `BROWSER_PROMPT`：禁止用 run_python_code 抓网页；403 → fetch_url_rendered；screenshot_url 工作流（装 playwright → install chromium 淘宝镜像 ~150MB → 截图）。

## 2.5 tools/ 工具集

### agent_tools.py —— 领域工具 + 请求级 ContextVar 定义点

- **四个 ContextVar**（L21-26）：`user_ip_var / user_lat_var / user_lon_var / user_id_var`——FastAPI 层请求开始时 set，工具内 `.get()` 读，多用户隔离的地基。
- `get_weather`：wttr.in 免费接口 `?format=j1&lang=zh`，每天 hourly 第 4 条（~12:00）当白天、第 7 条（~21:00）当夜晚。
- `get_user_location`：双源定位——浏览器 GPS 走 Nominatim 逆地理编码（必须带 UA），回退高德 IP 定位；处理了直辖市 city 为空/返回 list 的坑。
- `get_user_id`：**坑**——无上下文时 `random.choice(user_ids)` 返回随机假 ID（旧演示模式遗留）。
- `fill_context_for_report`：**空操作工具**，唯一作用是触发 middleware 把 `runtime.context["report"]=True` 从而切换报告 prompt——"用工具调用做信号"的技巧。
- `get_schedule`（L330-525）：课表 Excel 解析器，`pd.read_excel(file, header=2, index_col=0)`；内部闭包 `_parse_weeks`（"1-4,6-8,10" + 单双周过滤）、`_parse_course_cell`（`<br>` 分行、`◇` 分段、正则解析周次节次）。

### codexec_tools.py —— 代码沙箱

- Python 危险模式（os.system/subprocess/shutil.rmtree）**只告警不拦截**；shell 黑名单（`rm -rf`/`format`/`shutdown`/fork bomb）**默认拦截**。
- `run_python_code`：`subprocess.run([sys.executable, "-"], input=code, ...)`——**stdin 传代码（`python -`）避免临时文件**；Windows 用 `CREATE_NEW_PROCESS_GROUP` 便于整组 kill。
- `_exec_cwd()`：子进程 cwd = `workspace/{user_id}/{YYYYMMDD}/tasks/{plan_id}/`（任务级隔离）。
- `_truncate`：**掐头去尾保留两端**（各 2500 字符），比单纯截尾更利于 LLM 看到开头报错和结尾结果。
- `install_package`：uv → venv pip → `python -m pip` 三级回退，注入清华 PyPI 镜像，超时硬上限 120s。

### browser_tools.py —— 网页抓取

- bs4/playwright 双 try-import，缺失时正则降级，模块永远可 import。
- 完整 Chrome 120 指纹头 + 全局 Session 保 Cookie + 每次请求随机延迟 1~3s + 5xx 自动重试。
- **编码坑**：requests 无 charset 时默认 ISO-8859-1，回退 `resp.apparent_encoding`。
- **反检测体系**：`_STEALTH_JS` 抹掉 `navigator.webdriver`、伪装 languages/plugins；`_STEALTH_ARGS` 含 `--disable-blink-features=AutomationControlled`。
- `fetch_url` 失败信息含 403 时自动附"可尝试 fetch_url_rendered"——**给 LLM 的自愈线索**。
- `_ensure_chromium()` 两级检测（区分"未下载"和"下载不完整"），提示文案直接指导 LLM 调 install_package + `playwright install chromium` 自愈。
- import 本模块时通过 `sandbox_config` 副作用注入 `PLAYWRIGHT_DOWNLOAD_HOST`（淘宝镜像）和 `PLAYWRIGHT_BROWSERS_PATH`（避免 IDE 沙箱拦截 AppData 写入）。

### file_tools.py —— Word 模板填充

- 占位语法 `{内容提示|字体|字号}`，`TAG_PATTERN` 正则三组捕获。
- **东亚字体坑**：只设 `run.font.name` 对中文无效，必须操作 XML `w:rFonts` 的 `w:eastAsia` 属性。
- `_replace_in_single_paragraph` 是本文件最难的逻辑：处理 docx 的 **run 切片**问题——跨 run 时整段重建单 run、单 run 时**从右往左**替换避免偏移失效；仅当 run 恰好只有一个标签且占满整个 run 时才应用自定义字体。
- 中文字号映射表（初号 42pt → 八号 5pt）。
- 流程：提取文档上下文 → 收集全部标签 → 一次 LLM 批量生成 → 替换 → 存 `filled_{uuid8}.docx` → **删除模板文件**（破坏性！）→ 返回下载链接。

### middleware.py —— LangChain 中间件链

- `ToolCallLimitMiddleware`：工具调用超限 `exit_behavior="end"` 强制收尾。
- `skill_aware_monitor`（@wrap_tool_call）：限流时埋点 `log_rate_limit_event`；调工具时反查所属 SubAgent 把 name append 到 `runtime.context["active_skills"]`——**这就是 SubAgent 自动激活机制**；调用结束埋点写 `tool_call_logs`（失败静默）。
- `smart_prompt_switch`（@dynamic_prompt）：优先级 = report 场景 → active_skills 注入各 SubAgent prompt（带 `<!-- 已激活技能 [name] -->` 注释）→ 按已调用工具注入工具详情 → 裸 identity。
- **瑕疵**：函数名 `_log_tool_call埋点` 中英混用，合法但易踩编码坑。

### memory_tools.py / artifact_tools.py / search_tools.py / wordgen_tools.py

- memory：remember/recall/recall_one/forget 薄封装，四分型 fact/preference/project/schedule，软删除。
- artifact：**三个执行上下文 ContextVar 的定义点**（`current_plan_id_var` 等）；`create_artifact` 落盘 `UPLOAD_DIR/artifacts/` 后**绕过 database 层直接 pymysql INSERT**（导入了 `_db_create` 却没用，轻度坏味道）。
- search：极薄 Tavily 封装，`max_results=1` 是坑（结果单薄）。
- wordgen：`run_word_code` 生成"插 sys.path + import WordDoc"的前导代码与用户代码拼接，stdin 传给 `python -`。

### sandbox_config.py —— 沙箱策略

- `check_path_permission`：读全放行；写/删 **writable 优先于 readonly**（允许只读根内挖可写子目录，如 C:\ 只读但 workspace 可写）。注意文件头 docstring 写"readonly 优先"已过时，与实际代码矛盾。
- `_is_path_under`：`Path.resolve()` 后前缀比较，末尾补分隔符防 `C:\foo` 匹配 `C:\foobar`。

---

# 第三部分 · AIRAGAgent 支撑模块

## 3.1 kb/ —— 多知识库 RAG（kb_service 复用）

### kb/models.py —— 两张表的同步 CRUD

- `knowledge_bases`：scope=global/personal、owner_user_id、is_enabled、is_default（保护）、created_by。
- `kb_documents`：filename/stored_path/file_md5/version/status(pending→processing→ready/failed)/chunk_count。
- `_KB_SELECT` 用两个子查询**实时聚合** doc_count 和 chunk_count——不做冗余列，永远准确。
- `list_accessible_kb_ids(user_id)`（L86）：**检索权限核心**——全局启用库 + 该用户个人启用库。
- `delete_kb`：默认库受保护返回 `is_default_protected`；删除前把全部文档查出随返回值返回，**资源清理解耦到服务层**。
- `find_document_by_md5`：同库 MD5 查重（DB 有 `(kb_id, file_md5)` 索引）。
- `update_document_file`：替换时 `version+1`、status 重置 pending——version 是 Chroma 元数据溯源字段。
- 全部同步阻塞 + 短连接，由 FastAPI 路由层 `run_in_executor` 调用。

### kb/service.py —— 向量索引/检索/播种（重点）

- **懒加载 + 自愈合** `_get_vector_store()`（L47-93）：双检锁只初始化一次；初始化后 `probe_embeddings` 实测嵌入维度，与集合 metadata 里的 `provider:dim` 签名比对——**切换嵌入提供者时自动清空 kb_store 并后台线程全量重建**（`_rebuild_all_kbs`）。
- 分块：`RecursiveCharacterTextSplitter(chunk_size=200, chunk_overlap=20)`，separators 含中英文标点。
- `index_document`（L152-199）主流程：置 processing → 按扩展名分派加载器 → 分块 → **每 chunk 注入 `kb_id/doc_id/filename/version/chunk_index` 五个元数据**（多库隔离全靠 `where={"kb_id": ...}` 过滤，所有库共用一个 `kb_store` 集合）→ 锁内**先 delete(where doc_id) 再 add_documents**（幂等）→ 置 ready + chunk_count → 清 RAG 缓存。
- `index_document_async`：daemon 线程异步索引，上传接口立即返回，前端轮询 status。
- `search_with_scores`：单库 `{"kb_id": id}`、多库 `{"kb_id": {"$in": ids}}`；分数 clamp 到 [0,1]。
- `preview_chunks`：`store.get(where={"doc_id":...})` 拿回全部块按 chunk_index 排序后内存分页。
- `seed_default_kb()`：启动播种默认库——遍历 `data/` 目录，MD5 查重（数据库列，替代旧 md5.txt 机制），新文件登记 `source="system"` 并同步索引；失败不阻断启动。

### kb/embedding.py —— 嵌入提供者

- `OpenAICompatEmbeddings`：裸 requests 直连 `{base_url}/embeddings`，batch_size=20（百炼单批上限），构造时 base_url/key 为空直接 ValueError 快速失败。
- dashscope 分支 key 优先级 `embedding_api_key or api_key`；openai 分支读 chroma.yml。
- `probe_embeddings` 返回 `{ok, dim, latency_ms, error}`——自愈合检测的数据源。

## 3.2 rag/ —— 旧版 RAG 与总结服务

- `vector_store.py`：**遗留旧版**，单集合 `agent` + `md5.txt` 文件去重，构造即扫盘（非懒加载）。与新 `kb_store` 物理共存于 `chroma_ab` 目录。旧缺陷：`get_file_doucuments`（拼写错误）只识别 `.txt/.pdf/.xlsx` 漏了 `.xls`。
- `rag_service.py`：`RagSummarizeService` 是 knowledge SubAgent 的实际入口。检索前先 `list_accessible_kb_ids(user_id)` 拿可见库；**缓存键 `f"u{user_id or 0}|k{sig}|{query}"`**——用户 + 库签名 + 查询三要素，杜绝跨用户脏读；`rag_summarize` 现场组链 `prompt | print_prompt | model | StrOutputParser()`，model 用 `get_user_chat_model(user_id)` 支持用户自配模型。

## 3.3 model/factory.py —— 模型工厂

- **模块级单例**（L40-41）：`chat_model = ChatModelFactory().generator()`（`ChatDeepSeek(max_tokens=4096, temperature=0.7, request_timeout=240, max_retries=1)`）、`embed_model`——**import 即创建，改配置必须重启进程**。
- `get_user_chat_model(user_id)`（L73-110）：HTTP 调 user_service `/internal/user/model-config`（带 `X-Internal-Call: 1` 头），**刻意不缓存**（内部调用 <5ms，用户保存立即生效）。关键分支：base_url 含 `api.deepseek.com` 必须走 `ChatDeepSeek` 而非 `ChatOpenAI`——后者流式会丢弃 `delta.reasoning_content`，导致 thinking 事件为空。

## 3.4 database/ —— MySQL 层

### connection.py

- `get_db()`：pymysql + DictCursor + autocommit 的 contextmanager，**无连接池**（每次新建 TCP 连接，用简单换可靠）。
- `init_db()`：`CREATE TABLE IF NOT EXISTS` 建全量 14 张表（conversations/messages/users/login_devices/message_feedback/knowledge_bases/kb_documents/api_call_logs/tool_call_logs/rate_limit_events/plans/plan_steps/user_memories/artifacts）。
- **`_safe_add_column` / `_safe_add_index`**：直接执行 ALTER，**try/except 吞掉"已存在"错误**实现幂等迁移——简单粗暴但有效的 schema 演进方式。
- `_seed_admin_user`：播种 `admin/admin123`，PBKDF2-HMAC-SHA256（32 字节盐、10 万轮、`salt_hex:hash_hex`）。
- **mysql_conf patch 机制**：模块级 `mysql_conf` 字典在运行时可被微服务原地改写 `["database"]`——这是 db_patch.py 零侵入重定向的基础。

### models.py（约 160 行函数）要点

- `save_message`：新会话取首条消息前 30 字自动起标题；给已软删会话发消息**自动复活**（`is_deleted=0`）。
- `truncate_session_messages`：编辑重发/重新生成时回滚历史，完事调 `invalidate_session` 清 Redis 缓存——**DB 与缓存一致性闭环**。
- `branch_conversation`：对话分支，复制 `id <= branch_from_message_id` 的消息到新会话（`executemany` 批量），回读新消息直接返回免二次请求。
- `save_message_feedback`：靠 `uk_user_conv_role (user_id, conversation_id, message_role)` 唯一键 upsert，空串 = 取消（DELETE）。
- `get_schedule_settings`：**双数据源回退**——优先直连 lc_user.user_schedules，失败回退本库 users 表旧列。
- 埋点三剑客 `log_api_call/log_tool_call/log_rate_limit_event`：全部 try/except 吞异常——"埋点失败不影响主流程"。
- 看板查询：DAU/WAU/MAU 基于 `api_call_logs` 去重 user_id 且 `user_id > 0` 排除未登录。

## 3.5 config/ —— 六份 yml

- `rag.yml`：DeepSeek 对话模型名、各 API key 占位（环境变量注入）。
- `chroma.yml`：集合名 `agent`（旧）、persist `chroma_ab`、k=3、chunk 200/20、嵌入 provider=openai（百炼 compatible-mode）。
- `mysql.yml`：默认库名 `agent_records`（单体遗留，微服务下被 patch 覆盖）。
- `redis.yml`：连接池 20、session_cache.ttl=3600、rag_cache.ttl=1800、限流 chat 30 次/60s + tool 20 次/60s、任务队列 max_retries=3。
- `agent.yml`：sandbox 策略（C 盘只读、仅 workspace 可写、shell 黑名单、code_timeout_max=60、清华/淘宝镜像）。

**凭证注入机制**（`utils/config_handler.py` L47-54）：import 时用环境变量覆盖 yml 占位——`DEEPSEEK_API_KEY`/`EMBEDDING_API_KEY`/`TAVILY_API_KEY`/`GAODE_API_KEY`/`OPENAI_EMBEDDING_API_KEY`/`MYSQL_PASSWORD`。密钥不进 git。

## 3.6 utils/ 与 infrastructure/

- `paths.py`：`PROJECT_ROOT` = 仓库根；用户工作区 `workspace/{user_id}/{YYYYMMDD}/`；`cleanup_old_daily_workspaces` 只删 8 位纯数字且日期 >30 天的目录。**注意 `path_tool.py` 的 `get_project_root()` 是 AIRAGAgent 包目录，与 paths.py 差一级——两套并存的路径约定**。
- `prompt_loader.py`：`load_tool_details(tool_names)` 用正则从 tools_prompt.txt 按名切出子集——**SubAgent 只注入自己工具的文档，省 token**。
- `logger_handler.py`：按日期日志文件，import 时自动清 7 天前旧日志。
- `redis_client.py`：懒加载连接池 + `is_redis_available()` ping 探测——**全层降级开关，Redis 挂了缓存/限流/队列全部静默跳过**。
- `session_cache.py`：键 `session:{user_id}:{session_id}`，命中滑动续期。
- `rag_cache.py`：SCAN 游标分批删前缀键（不用 KEYS，不阻塞 Redis）。
- `rate_limiter.py`：**ZSET 滑动窗口**限流（zremrangebyscore 清窗 → zcard 计数 → zadd → expire），Redis 挂了**放行**（fail-open，限流是保护不是门禁）。
- `task_queue.py`：Redis 队列，三级优先级取任务（到期重试 → priority ZSET → 普通 LPOP）；`plan_execute_handler` 后台跑 DeepAgent plan。

## 3.7 skills/ —— 旧技能体系

`SkillRegistry` 单例（`_skills`/`_active_skills` 双 dict），注册 7 个 skill。这是 SubAgent 体系的前身，`middleware.py` 的 `_find_agent_by_tool` 会先查 SubAgentRegistry、失败回退 SkillRegistry——**新旧体系共存期的兼容设计**。

---

# 第四部分 · services/ —— 六个微服务

## 4.0 公共 core/ 库（每服务内嵌一份副本，绝对解耦）

设计哲学：**每个服务自带 core/ 副本，可脱离项目根目录独立部署**（注释原文），无共享包依赖。

| 文件 | 要点 |
|---|---|
| `config.py` | 纯环境变量；端口/库名兼容新旧变量名（`PORT or AUTH_PORT or 8001`）；`get_service_url(name)` 是**白名单式**跨服务地址表（auth 只许调 user，admin 许调 auth/user/chat，未登记直接 raise） |
| `base_app.py` | `create_app(service_name, on_startup, on_shutdown)`：CORS 全放开、lifespan、内置 `GET /api/health` |
| `db.py` | pymysql 短连接 contextmanager；`ensure_database_exists()` |
| `jwt_auth.py` | **共享密钥独立验签，不查库**（"JWT 有效即视为可信"）；`get_current_user` 支持 **Bearer 头 + `?token=` query 两种方式**（后者为 SSE/文件下载预留）；`get_admin_user` = role=admin + IP 白名单；`get_client_ip` 依次取 X-Real-IP → X-Forwarded-For → client.host |
| `http_client.py` | `ServiceClient`（httpx.AsyncClient，15s 超时，自动带 `X-Internal-Call: 1` 头，**服务间 127.0.0.1 直连不走 nginx**） |
| `redis_client.py` | 全局单例连接池，`is_redis_available()` ping 探测 |
| `events.py` | **仅 chat 和 admin 有**：三个频道 `events:chat.completed` / `events:rate.limited` / `events:tool.called`；`subscribe_events` daemon 线程阻塞循环 + threading.Event 优雅停止 |

**core 差异**：chat 没有 http_client（不主动调别的服务，引擎直连 user_service）但有 events；kb 没有 http_client/events（纯被调方）；file 没有 db/http_client/events（文件系统即存储）。

## 4.1 auth_service（:8001，lc_auth）

15 个路由：注册/登录/me/密保找回（question + reset）/改密/密保设置/设备三件套（list/delete/revoke-all-others）+ 5 个内部接口。

**关键逻辑**：
- **register/login**：同步 DB 操作一律 `run_in_executor` 包装不阻塞事件循环；签发 JWT 时生成 `device_token = secrets.token_hex(16)` 写入 payload 并落 `login_devices` 表；注册后调 user_service `/internal/user/profile` 同步建档——**失败仅 warn（最终一致性）**。
- **密码哈希**：PBKDF2-HMAC-SHA256，32 字节随机盐，10 万轮，`salt_hex:hash_hex` 存储。密保答案 strip+lower 后同算法（大小写不敏感）。
- **admin 保护**：登录时 admin 需过 IP 白名单；`delete_user`/`reset_user_password` 遇 role=admin 直接拒绝。
- **设备管理**：`ON DUPLICATE KEY UPDATE`（uk=device_token）实现同 token 再登录刷新活跃并取消吊销；UA 粗解析 OS/浏览器/设备类型。

建表：`users`（username UNIQUE）、`security_questions`（user_id UNIQUE 一人一题）、`login_devices`（uk_device_token + is_revoked 软下线）。

## 4.2 user_service（:8002，lc_user）

**三表均以 user_id 为主键**（与 auth.users.id 逻辑外键、物理分离）：`user_profiles` / `user_schedules` / `user_models`。

- **头像**：类型白名单双校验（content_type + 扩展名），≤5MB，`avatar_{user_id}_{uuid8}{ext}`，upsert 更新；静态路由防路径穿越（拒绝含 `/`、`\`、`..` 的文件名）。
- **课表上传五步流水线**：后缀校验 → ≤2MB → start_date 格式校验 → **pandas 预校验 Excel 结构**（`header=2, index_col=0`，必须含星期一~星期天七列，缺列给中文清单）→ 覆盖式保存固定文件名 `schedule_{user_id}.xlsx`。
- **模型配置**：对外 GET 走 `_mask_api_key` 脱敏（`sk-abcd****wxyz`）；`/internal/user/model-config` 返回明文 key，**是全项目唯一带 `X-Internal-Call` 头校验的内部接口**。
- **注销账号 saga**：① auth 删认证（admin 被拦截即终止）→ ② 清课表文件 + 三表 → ③ 尽力而为调 chat 清会话。**已知 bug**：调的是 `/internal/chat/users/{id}/data` 而 chat 实际暴露 `/cleanup`，404 被 warn 吞掉。

## 4.3 chat_service（:8003，lc_chat）—— 核心服务

### main.py：`POST /api/chat` 处理流程

1. 手动解析 client_ip（X-Real-IP → X-Forwarded-For → client.host）。
2. **set 四个 ContextVar**（user_ip/lat/lon/user_id）。
3. **限流**：`get_chat_rate_limiter().is_allowed(client_ip)`（ZSET 滑动窗口 30 次/60s，fail-open）；超限发 `rate.limited` 事件 + 429。
4. 有 `uploaded_file_path` 时在消息前拼 `[用户已上传Word文件，服务器路径为：...]`。
5. 流式返回 `StreamingResponse(generate_sse_stream(...), media_type="text/event-stream")`，响应头：
   ```python
   {"Cache-Control": "no-cache", "X-Accel-Buffering": "no",  # 关 nginx 缓冲，SSE 必需
    "X-RateLimit-Limit": ..., "X-RateLimit-Remaining": ...}
   ```
   SSE 帧 `data: {json}\n\n`，末尾 `data: [DONE]\n\n`。

启动钩子：`init_db()`（幂等建表）→ `register_default_handlers()`（Redis 任务队列）→ `cleanup_old_daily_workspaces()`（清 30 天前日期目录）。

### agent.py：同步生成器 → 异步流的桥接（经典问题）

```python
queue: asyncio.Queue = asyncio.Queue()
SENTINEL = object()
ctx = contextvars.copy_context()                     # 拷贝请求上下文
def sync_producer():
    for chunk in self.agent.execute_stream(...):     # 同步生成器在线程池里跑
        loop.call_soon_threadsafe(queue.put_nowait, chunk)   # 跨线程安全入队
    loop.call_soon_threadsafe(queue.put_nowait, SENTINEL)
loop.run_in_executor(None, lambda: ctx.run(sync_producer))
```

三个要点：
- **`loop.call_soon_threadsafe(queue.put_nowait, item)`**：工作线程不能直接 await queue.put，把入队调度回事件循环线程——线程→asyncio 的标准桥。
- **`ctx.run(sync_producer)`**：保证工作线程里 `user_ip_var.get()` 等读到请求上下文。
- **哨兵 + 错误透传**：异常也入队，消费侧 `isinstance(item, Exception)` 时置错误标记并 raise，埋点能记录失败。

**finally 埋点**：无论正常/异常/断连，算 duration_ms + 粗估 tokens（中文 1 字 = 1 token，其余 4 字符 = 1 token）→ `log_api_call` 写 lc_chat → `publish_event(CHANNEL_CHAT_COMPLETED)` 发 admin。埋点自身再套 try/except。

**会话持久化**：支持 `truncate_to` 历史回滚；结束后 save_message 写 user/assistant 两条 + 回写 Redis 缓存 + 补发 `message_ids` 事件（前端用于反馈/分叉）。

### db_patch.py：零侵入 DB 重定向（全架构最巧妙的一笔）

```python
_conn.mysql_conf["database"] = DB_NAME   # 原地改字典！AIRAGAgent 几千行代码零改动落到 lc_chat
```

- 必须在 import AIRAGAgent 业务模块**之前**完成。
- 同时替换 `log_rate_limit_event` 为发 Redis 事件版本（表在 lc_admin 不在 lc_chat）；包装 `log_tool_call` 先写库再发事件。
- `_DB_PATCHED` 标志保证幂等。

### 其余路由

会话 CRUD/search/branch/meta、feedback、`POST /api/plan`（stream 或 background 投 Redis 队列）、`GET /api/plans`、`GET /api/subagents`（前端能力卡片）、`GET /api/tasks/{task_id}`（后台任务轮询）、内部接口（admin 查会话、注销清理）。

## 4.4 kb_service（:8004，lc_kb）

28 个路由，三层结构：**路由层 → kb_models（MySQL 元数据）+ AIRAGAgent kb/service（Chroma 向量）**。

- 模块加载时直接 `_conn.mysql_conf["database"] = DB_NAME` 重定向（与 chat 同套路）。
- **管理端** `/api/admin/kb/*`（get_admin_user）：list/create/patch/delete、文档 CRUD、chunks 预览（limit≤100）、rebuild、search-test、embedding-status。
- **用户端** `/api/kb/mine/*`：个人库上限 20 个、单次上传 ≤20 文件、归属校验 `_user_kb_or_403`；`GET /api/kb/mine` 同时返回 mine + 启用的 global_kbs。
- **内部** `/internal/kb/search`：供 chat 的 RAG 检索。

**上传流水线（MD5 去重）**：

```python
rel_path, md5 = _save_upload(kb_id, content, ext)   # 存 uploads/kb/{kb_id}/{md5}.{ext}，内容寻址
dup = kb_models.find_document_by_md5(kb_id, md5)    # 库内查重
if dup: return {"ok": False, "error": "与现有文档「...」内容重复，已跳过"}
doc = kb_models.create_document(...)
kb_service.index_document_async(doc["id"])          # 后台线程索引，前端轮询 status
```

- 替换：MD5 相同直接 400"无需替换"；否则 version+1 → 删旧物理文件 → 异步重索引。
- 删除：先删 Chroma 向量（`where={"doc_id":...}`）→ 删 DB → 删物理文件，`_delete_physical_file` 用 `os.path.commonpath` 白名单防护防误删。
- 启动钩子：`seed_default_kb()` 播种默认库。
- 改 `is_enabled` 后调 `_invalidate_cache()` 刷新 RAG 检索缓存。

## 4.5 admin_service（:8005，lc_admin）

**双重角色**：管理后台 API + Redis 事件订阅落库方。

- `on_startup` 注册三个订阅（daemon 线程 `pubsub.listen()` 阻塞循环）：`chat.completed → api_call_logs`、`tool.called → tool_call_logs`、`rate.limited → rate_limit_events`。**lc_admin 三表是 lc_chat 的最终一致副本**——事件丢失可接受（"统计略有偏差"），换来统计查询不打业务库。
- 设计瑕疵：`pubsub.listen()` 阻塞迭代，stop_event 要等下一条消息才被检查到——但线程是 daemon，进程退出不受影响。
- 路由（全部 get_admin_user）：用户列表（auth 拉全量 + `asyncio.gather` 并发调 user 补资料）、用户会话（透传 chat）、删用户三步级联、重置密码（默认 `123456789`）、看板四接口（overview/trends/tools/top，days 钳 [1,365]）。
- 跨服务调用统一从 `Authorization` 头剥 Bearer 后**透传 JWT**。
- **已知妥协**：`get_dashboard_top_questions` 恒返回空——消息内容在 lc_chat，需要 chat 提供内部接口或新增事件频道才能启用。

## 4.6 file_service（:8006，无 DB）

- `POST /api/upload-word` / `GET /api/download/{filename}`：**无鉴权**（依赖内网/网关隔离，"内部接口"思路延伸到公开路由——值得复盘的安全点）。
- 工作区三接口（list/download/preview）JWT 鉴权，`_resolve_workspace_path` 防穿越：反斜杠归一 → 拒绝 `..` → `Path.resolve()` 后 `relative_to(workspace/{user_id})` 校验。
- `.playwright` 及所有 `.` 开头条目隐藏；条目带 `is_image`/`is_text` 标记供前端决定 `<img>` 直显或文本预览。

## 4.7 start.bat 差异

六个服务各一份，统一套路（优先项目根 .venv）。**chat 和 kb 多两行**：`set AGENT_HOME=..\..` 和 `set PYTHONPATH=%AGENT_HOME%;%PYTHONPATH%`——因为它们要 import 项目根的 AIRAGAgent 引擎；其余四个完全自包含。

---

# 第五部分 · frontend/ —— Vue 3 前端

> **先说一个与 README 不符的事实**：markdown-it、vue-virtual-scroller 在 package.json 里但**全库无任何 import**（冗余依赖）；Markdown 渲染是自研零依赖渲染器 `utils/markdown.js`；没有 defineAsyncComponent、没有 manualChunks、没有 50ms 节流。懒加载靠**路由级 `() => import()` 动态导入**。

## 5.1 工程配置

- `vite.config.js`：双模式代理——设了 `VITE_API_TARGET` 全部 `/api` 走单一目标（生产 nginx）；否则 `microserviceProxy()` 按 URL 前缀分发到 8001-8006。**vite proxy key 为前缀匹配按定义顺序**，所以 `/api/user/password|security-question|devices`（→8001 auth）必须放在 `/api/user/profile|avatar|schedule|model|account`（→8002 user）之前。
- `main.js`：**先 `initTheme()` 再 mount**——首帧渲染前写 `data-theme`，避免主题闪烁（FOUC）。
- `router/index.js`：6 条路由全部动态导入；守卫用模块级 `tokenVerified` 缓存避免每次跳转打 `/api/auth/me`；网络异常放行（"避免离线时被卡住"）；**角色互斥**——admin 登录后访问 `/` 重定向到 `/admin`，反之亦然；未登录访问带 `?redirect=` 供登录后回跳。

## 5.2 api/chat.js —— SSE 消费核心

**不是 EventSource，而是 `fetch + ReadableStream`**（需要 POST + Authorization 头，EventSource 做不到）：

```js
const reader = response.body.getReader()
buffer += decoder.decode(value, { stream: true })   // 防多字节字符截断
const lines = buffer.split('\n')
buffer = lines.pop() || ''                          // 末行不完整留到下一轮
for (const line of lines) {
  if (line.startsWith('data: ')) {
    const data = line.substring(6).trim()
    if (data === '[DONE]') continue
    const parsed = JSON.parse(data)                 // 失败兜底：原文当 output 追加
    ...
  }
}
```

事件分发：`thinking → onThinking`、`output → onOutput(delta, fullOutput)`（**同时传增量和全量**，useChat 用全量直接覆盖天然防丢字）、`message_ids`、`plan_*`/`step_*` 前缀透传给可选 `onEvent`（不传 onEvent 的旧调用方零改动兼容）。401 → logout 跳登录；429 → 抛限流文案；`AbortError` 直接上抛供"停止生成"识别。

请求体：`{message, session_id, stream:true}` + 可选 `latitude/longitude`、`uploaded_file_path`、`truncate_to`。

## 5.3 useChat.js —— 全应用状态中枢（866 行，无 Pinia）

**最重要的设计决策——模块级单例**：

```js
// 放到模块作用域，ChatView 路由切换（mount/unmount）时不销毁。
// 否则流式生成中切到"我的知识库"再返回，pendingStreams 会丢失
const pendingStreams = ref(new Map())  // sessionId -> { controller, streamingContent, thinkingContent, ... }
```

**天然支持多会话并发流式**——A 会话还在生成时切到 B 会话，A 的流在后台继续跑。

### sendMessage 流程要点

1. 防重入：同一会话流式中禁止再发。
2. push user 消息 → 自动标题（首条前 30 字，除非用户手动改过）→ bump 置顶。
3. push reactive assistantMsg 占位 `{streaming:true, plan:null, steps:{}}`。
4. `markRaw(new AbortController())`——**markRaw 避免 Vue 对 controller 做响应式代理**。
5. 首次发送用 `navigator.geolocation` 拿经纬度（10s 超时，失败缓存不再重复请求）。
6. `onMessageIds` 把后端真实消息 id 存到 **`dbId` 字段而非覆盖 `id`**——覆盖 `id` 会让 `v-for :key` 变化导致组件重挂载，"流式结束后内容短暂消失再出现的闪烁"。
7. `onEvent` 里是 **step 状态机**：plan_created 初始化每 step 的 `{status:'pending', thinking:'', output:''}`；step_started → running；step_thinking/output 增量追加；step_completed → done；step_failed → failed。

### 前后端历史一致性

- `regenerateMessage(assistantMsg)`：**向前找最近的 user 消息**作为问题（否则 `[U1,A1,U2,A2]` 重生成 A1 会错把 U2 当问题）。
- `regenerateFromUserIndex` / `editAndResend`：本地 `splice` 截断 + `truncate_to` 发后端同步回滚，**前后端上下文一致**。

### 防丢失三件套

1. 模块级单例防路由切换丢失；
2. `loadConversations` 单次加载保护 + **先把正在流式的本地会话摘出来，后端数据填充后再覆盖回去**——防后端旧数据覆盖进行中内容；
3. `resetChatState()` 登出/注销/登录时 abort 全部流并清空——防串号看到上一个用户的会话。

## 5.4 视图与组件要点

- **ChatView.vue**（~2000 行）：侧边栏（搜索 220ms 防抖、会话五操作、分支会话 `↳` 标记）+ TransitionGroup 消息流 + 5 个 SideDrawer（知识库/工作区/计划历史/Agent 能力/设置）。滚动管理完整：80px 阈值判 isAtBottom、三个 watch 自动滚底、不在底部累计 newMessageCount 显示 FAB。鼠标光晕用 **requestAnimationFrame 合并 mousemove** 只改 transform（合成层不触发布局）。
- **MessageBubble.vue**：`computed(() => renderMarkdown(content))`——流式期间每个 delta 触发完整重渲染，靠自研渲染器足够轻（纯正则无 AST）保证性能。代码块复制用**事件委托**（`e.target.closest('.code-copy-btn')` + `data-copy` 属性 encodeURIComponent 注入）。思考过程 `<details open>` 折叠面板。
- **PlanPanel.vue**：静态 plan 结构 + 流式 steps map 合并渲染；四态视觉（running 旋转/done ✓/failed ✗/pending ○）。
- **KbManager.vue**（~2000 行，最复杂组件，scope prop 一套代码三场景）：拖拽用 **`dragDepth` 计数器**（dragenter++/dragleave--）解决子元素进出遮罩闪烁；上传进度用 **XMLHttpRequest**（fetch 拿不到 upload progress），进度到 1 后切 indeterminate 表示"服务器处理中（写入+索引）"；索引状态 2s 轮询；分块预览分页；检索 playground 带相似度百分比条。
- **file.js**：下载/预览带 Bearer token 不能 `<a href>` 直链，走 fetch → blob → `URL.createObjectURL` → 动态 `<a download>` 点击 → `revokeObjectURL` 完整链路。
- **utils/markdown.js**：自研渲染器管线——代码块/公式先抽占位（`\u0000CODEBLOCK_n\u0000`）→ HTML 转义 → 逐行状态机（GFM 表格需当前行含 `|` 且下一行是 `|---|`）→ 还原占位。`renderMath()` 极简 LaTeX→HTML（上下标/frac/sqrt/希腊字母），复杂公式兜底保留原文。
- **global.css**："黑白极简 · 纸与墨"设计系统——`#fafafa` 纸面 + `#161616` 墨黑，`[data-theme="dark"]` 整套变量反转；玻璃拟态三档、`body::after` 叠 SVG feTurbulence 噪点纹理（opacity 0.04）纸面质感。

**死代码**：`WelcomeScreen.vue` 全库无引用（ChatView 内联了自己的 welcome hero）。

---

# 第六部分 · 部署 / 迁移 / 构建 / 测试

## 6.1 deploy/

- **`deploy_to_server.py`**：paramiko 裸机部署到 `81.70.100.57`。凭据全走环境变量（`LC_SERVER_*`，未设密码直接 SystemExit，不硬编码）。6 步流程：建连 → 环境探查 → **`sudo rm -rf /opt/lc-course/backend` 全量覆盖**（非增量！）→ SFTP 递归上传 → 前端全量替换 `/var/www/lc-course-frontend` → nginx 配置自适应（探测 Debian `sites-available` 还是 RHEL `conf.d` 风格）。`sudo_cmd` 用 `echo '密码' | sudo -S` 提权。"自动上传 + 手动启动"半自动模式。
- **`start_microservices.sh/.bat`**：`set -a; . .env; set +a` 导出环境 → `uv sync --frozen` → `export PYTHONPATH=项目根` → 逐服务 `nohup uvicorn main:app --app-dir .` 后台启动，PID 写 `AIRAGAgent/logs/`。stop 脚本遍历 PID 文件 kill。
- **`nginx.conf`（前端）**：SPA 回退 `try_files $uri $uri/ /index.html`；`/assets/` 30 天 immutable 缓存；**SSE 专项**：`proxy_buffering off; proxy_cache off; proxy_read_timeout 3600s`。
- **`nginx_microservices.conf`**：微服务路由表——`/api/user/` 前缀被 auth/user 两服务共享（password|security-question|devices → 8001，profile|avatar|schedule|model|account → 8002），正则优先级体现了这个设计。

## 6.2 migrations/ —— 一套 alembic env 跑 5 个库

- `env.py` 用 **`-x target=<auth|user|chat|kb|admin>`** 命令行参数动态切换：`version_table = alembic_version_<target>`（每库独立版本表）+ `version_locations = migrations/versions/<target>`（每库独立迁移链）。
- 每个 target 只有一个 `0001_baseline.py`，`branch_labels=("<target>",)`——所以 migrator 用 `alembic -x target=$t upgrade ${t}@head` 寻址消除多 head 歧义。
- **无 ORM，migration 用 `op.execute()` 写 raw SQL**，全部 `CREATE TABLE IF NOT EXISTS` 幂等。
- chat 库 baseline 最重（9 张表）；`plan_steps.plan_id` 有**全项目唯一显式外键** `ON DELETE CASCADE`。

## 6.3 Dockerfile —— 多阶段构建

1. `deps`：python:3.13-slim + 中文字体 + `uv export --frozen` 从 uv.lock 导出精确依赖（可复现）。
2. `agent-deps`：装 Playwright Chromium（npmmirror 加速），仅 chat/kb 需要。
3. `code-light`（auth/user/admin/file）：只 COPY `services/${SERVICE}/`，镜像最小；`CMD exec ... uvicorn`（exec 让 uvicorn 接管 PID 1 正确收 SIGTERM）。
4. `code-agent`（chat/kb）：`COPY . /app/`（整个 AIRAGAgent 引擎）+ `PYTHONPATH=/app`。

## 6.4 tests/ —— pytest 体系

- **顶层 conftest 的隔离加载**：各服务 `main.py` 用服务内相对导入（`from core import ...`），不同服务的 `core`/`models` 包名相同会互相污染——`load_service_main` 先删 `sys.modules` 残留的 `core*`/`models*` 缓存，再以唯一模块名加载。建议按服务分目录跑：`pytest tests/auth_service/ -v`。
- **测试库隔离**：`lc_auth_test`/`lc_kb_test` 独立库，session 级建表（内嵌 SCHEMA_SQL 与生产对齐），每用例 TRUNCATE 清理；外部依赖全 mock（auth 屏蔽跨服务调用、kb 屏蔽全部 Chroma/嵌入调用换同步 no-op）。
- **覆盖**：auth 5 文件（注册登录流程/账号安全/设备/内部接口含"不能删 admin"保护/health）；kb 4 文件（管理端 CRUD/用户端 CRUD/越权矩阵/health）；另有 user/chat/admin/file/agent_core（orchestrator、plan_logic、sub_agent 注册表、rate_limiter 纯单测）。
- pytest 配置：`addopts = "-p no:langsmith"`（langsmith 插件与 pydantic 版本不兼容）。

## 6.5 其他目录

- `static/`：单体模式的前端托管备用（`SERVE_STATIC=true`）。
- `uploads/`：`avatars/`、`kb/{kb_id}/{md5}.{ext}`（内容寻址去重）、`artifacts/`、`screenshots/`。
- `workspace/`：`{user_id}/{YYYYMMDD}/tasks/{plan_id}/` 三级隔离 + `.playwright/` 浏览器二进制。
- `chroma_ab/`：Chroma 持久化（`agent` 旧集合 + `kb_store` 新集合共存）。
- `services/migrate_to_microservices.py`：历史一次性脚本，把旧单体库 `agent_records` 拆成 5 个微服务库（幂等可重跑、保留原自增 ID、保留旧库做回滚备份）。

---

# 第七部分 · 贯穿全局的十大设计精髓（复习重点）

1. **ContextVar 链**：FastAPI 注入用户上下文（user_id/ip/lat/lon）→ Orchestrator 注入计划上下文（plan_id/session_id/step_idx）→ 工具层消费；并行时 `copy_context().copy()` 跨线程传递（Context 非线程安全，直接共享报 "already entered"）。
2. **同步生成器 → 异步 SSE 桥**：`asyncio.Queue` + `loop.call_soon_threadsafe(queue.put_nowait, item)` + `ctx.run(sync_producer)` + SENTINEL 哨兵。
3. **db_patch 零侵入重定向**：`_conn.mysql_conf["database"] = DB_NAME` 原地改字典，几千行引擎代码零改动落到微服务库。
4. **generator 既流式又返回值**：`next()` 循环 + `StopIteration.value` 取 PlanSpec。
5. **多知识库隔离三处落地**：MySQL 按 owner_user_id/scope、Chroma 按 `kb_id` 元数据 filter（单集合 kb_store）、Redis 缓存键内嵌 `u{uid}|k{sig}` 签名。
6. **事件驱动统计**：chat 生产 → Redis Pub/Sub → admin 消费落库，lc_admin 三表是最终一致副本，统计查询不打业务库。
7. **降级哲学（可用性 > 一致性）**：Redis 挂了缓存/限流/队列全部静默放行或跳过；埋点写库失败吞异常；kb 播种失败不阻断启动；嵌入切换自动清库重建自愈。
8. **JWT 共享密钥独立验签**：不查库不回调 auth；Bearer + `?token=` 双通道（SSE/img 场景）；`/internal/*` 无鉴权靠内网拓扑隔离（唯 model-config 带 `X-Internal-Call` 头，因为返回明文 api_key）。
9. **LLM 友好设计**：403 错误信息附"可尝试 fetch_url_rendered"自愈线索；SubAgent 只注入自己工具的文档（省 token）； Planner 纯文本 JSON（DeepSeek thinking 不支持 tool_choice）+ `_extract_json` 容错。
10. **前端防丢失三件套**：useChat 模块级单例、流式会话合并回写、`dbId` 与 `id` 分离防 v-for key 闪烁。

---

# 第八部分 · 已知坑与瑕疵清单（复盘用）

| # | 位置 | 问题 |
|---|------|------|
| 1 | user_service 注销 | 调 chat 用 `/internal/chat/users/{id}/data`，实际暴露的是 `/cleanup`——404 被 warn 吞掉，**会话数据其实没清** |
| 2 | admin_service | `top_questions` 恒返回空（消息内容在 lc_chat） |
| 3 | file_service | `/api/upload-word`、`/api/download/{filename}` 无鉴权，靠网关隔离 |
| 4 | sandbox_config.py | docstring 说"readonly 优先"，实际代码是 writable 优先 |
| 5 | file_tools.py | `auto_fill_word` 成功后**删除模板文件**（破坏性） |
| 6 | agent_tools.py | `get_user_id` 无上下文时返回随机假 ID（旧演示遗留） |
| 7 | artifact_tools.py | 导入了 `_db_create` 却绕过直接 pymysql INSERT（坏味道） |
| 8 | middleware.py | 函数名 `_log_tool_call埋点` 中英混用 |
| 9 | frontend | markdown-it、vue-virtual-scroller 冗余依赖；WelcomeScreen.vue 死代码；Login.vue 无找回密码 UI（API 层已就绪） |
| 10 | admin_service | 事件订阅线程要等下一条消息才响应 stop_event（daemon 线程兜底，无实际影响） |
| 11 | rag/vector_store.py | 旧版遗留：`get_file_doucuments` 拼写错误、漏 `.xls`；`file_handler.listdir_with_allowed_type` 路径非法时返回值类型错误 |
| 12 | search_tools.py | Tavily `max_results=1` 结果单薄 |
| 13 | model/factory.py | import 即创建单例，改配置必须重启进程 |
| 14 | chat_service init_db | 会把单体时代全部 14 张表建到 lc_chat（冗余但无害；权威 schema 是 alembic baseline） |

---

# 第九部分 · 一条请求的完整生命周期（串线复习）

以「用户问：帮我查下北京天气，写进 Word 报告」为例：

1. **前端** ChatInput → `useChat.sendMessage` → push user 消息 → `api/chat.js` fetch POST `/api/chat`（Bearer + 经纬度）。
2. **chat_service/main.py**：解析 IP → set 4 个 ContextVar → ZSET 滑动窗口限流 → `StreamingResponse`。
3. **chat_service/agent.py**：`copy_context()` → 线程池跑 `Orchestrator.execute_stream` → `call_soon_threadsafe` 入队 → 消费侧逐 chunk yield SSE。
4. **Orchestrator**：召回长期记忆 → **Planner** 流式思考（前端 thinking 面板实时滚动）→ 解析 JSON 出 2 步计划（step0 weather 查天气；step1 wordgen 写报告，depends_on=[0]）→ 落库 plans/plan_steps → yield plan_created。
5. **Executor**：step0 ready → `SubAgentRunner(weather)` → `create_agent` + 中间件链 → get_user_location/get_city_code/get_weather（工具内 `user_lat_var.get()` 读 GPS）→ 结果落盘 `plan_results/step_0.md` → Reflector accept。
6. step1 依赖满足 → wordgen 执行（任务描述里注入了 step_0.md 路径+摘要）→ `run_word_code` 子进程 cwd=`tasks/<plan_id>/` → 读前序结果 → WordDoc 排版 → doc.save → `create_artifact` 注册产物。
7. **Finalizer** 汇总两步结果流式输出 → yield plan_completed。
8. **agent.py finally**：save_message 落库两条 + 回写 Redis 缓存 + 发 message_ids → 埋点 log_api_call + publish chat.completed。
9. **admin_service**：订阅到事件 → INSERT lc_admin.api_call_logs（看板 DAU/趋势数据源）。
10. **前端**：step 状态机驱动 PlanPanel 实时渲染 → 全量 output 覆盖 assistantMsg → onDone 持久化会话。

---

# 附录 · 复习自测清单

能回答这些问题，说明项目已经捡回来了：

- [ ] Planner 为什么不用 `with_structured_output`？（DeepSeek thinking 不支持 tool_choice，纯文本 JSON + _extract_json 容错）
- [ ] 并行执行时 ContextVar 为什么要 `parent_ctx.copy()`？（Context 非线程安全，共享报 already entered）
- [ ] 同步 Orchestrator 怎么接进 async FastAPI？（Queue + call_soon_threadsafe + ctx.run + 哨兵）
- [ ] db_patch 的原理？（原地改 mysql_conf 字典，import 前完成）
- [ ] 多知识库在 Chroma 里怎么隔离？（单集合 kb_store + chunk 元数据 kb_id/doc_id + where 过滤）
- [ ] 切换嵌入模型会发生什么？（维度签名不匹配 → 自动清库 → 后台全量重建）
- [ ] JWT 为什么能各服务独立验签？internal 接口安全靠什么？（共享密钥不查库；内网拓扑 + nginx 不转发）
- [ ] 限流 Redis 挂了会怎样？（fail-open 放行）
- [ ] 前端流式中切换路由为什么内容不丢？（useChat 模块级单例 + pendingStreams 按会话隔离）
- [ ] 重新生成消息时前后端历史怎么保持一致？（本地 splice + truncate_to 后端回滚 + 向前定位最近 user 消息）

---

*手册生成时间：2026-08-05 · 基于当前代码库实读，行号与实现以代码为准。*





