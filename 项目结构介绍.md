# lc-course 项目结构介绍

## 项目概述

本项目是一个基于 **微服务架构 + Supervisor-Worker 多智能体编排** 的**自动化办公助手**。

- **后端**：6 个独立 FastAPI 微服务（每个服务自带 `core/` 库，绝对解耦），通过 Nginx 按 URL 前缀路由；服务间用 HTTP + JWT 通信，事件流走 Redis Pub/Sub。
- **智能体引擎**：`AIRAGAgent` 包提供 `Orchestrator`（Supervisor）+ `SubAgent`（Worker）的编排框架，由 `chat_service` 承载并暴露 `/api/chat` SSE 流式入口。
- **前端**：Vue 3 + Vite，对接 SSE 流式输出、知识库管理、计划可观测面板。
- **能力**：RAG 多知识库、联网搜索、代码沙箱执行、网页抓取、Word 文档填充、产物管理、长期记忆、课表/天气查询等。

> **架构演进说明**：旧版采用 Streamlit + `SupervisorAgent` + `ReactAgent` 两层单体结构（`app.py` / `fastapi_app/` / `react_agent.py` / `supervisor_agent.py`），均已废弃。当前为微服务 + Orchestrator 四阶段范式（Planner → Executor → Reflector → Finalizer）。

---

## 一、根目录

| 文件/目录 | 说明 |
|-----------|------|
| `README.md` | 项目总览、架构图、技术选型、本地启动指南 |
| `项目结构介绍.md` | 本文档 |
| `pyproject.toml` | uv 包管理配置，项目名 `Rovio`，Python ≥ 3.13 |
| `uv.lock` | 依赖锁定文件 |
| `langgraph.json` | LangGraph 配置（历史保留，微服务架构下不再作为运行入口） |
| `.gitignore` | Git 忽略规则（已排除 logs/chroma_ab/uploads/workspace/.playwright 等运行时产物） |
| `agent头像.jpg` | 智能体头像素材 |

---

## 二、AIRAGAgent/ — 智能体引擎包

被 `chat_service` 和 `kb_service` 复用的核心 Python 包。包含编排器、SubAgent 体系、工具集、RAG、模型工厂、配置、数据模型等。

### agent/ — 编排核心（Supervisor-Worker）

| 文件 | 说明 |
|------|------|
| `orchestrator.py` | **Orchestrator（Supervisor）主循环**。替代旧版 `SupervisorAgent` + `ReactAgent`。四阶段范式：<br>① **Planner** `_plan_stream()` —— 流式推送 `reasoning_content` 思考过程，纯文本 JSON 输出（DeepSeek thinking 模式不支持 `tool_choice`），解析成 `PlanSpec`，持久化到 `plans` + `plan_steps` 表<br>② **Executor** `_execute_step()` / `_execute_steps_parallel()` —— 单步串行或多步并行（`ThreadPoolExecutor` max=5，`ContextVar` 按线程 `copy_context()` 避免竞态）；`subagent` 为空时由 Orchestrator LLM 直接回答，非空时委派 `SubAgentRunner`<br>③ **Reflector** `_reflect()` —— 评估单步结果，action 取值 `accept` / `retry(≤1)` / `revise(≤2)` / `ask_user`；修订时调 `_revise_plan` 整体替换 DB 里的 step 列表<br>④ **Finalizer** `_finalize()` —— 单步复用 step.result，多步 LLM 整合<br><br>**跨步骤数据交接**：`_save_step_result` 每步结果落盘到 `tasks/<plan_id>/plan_results/step_<idx>.md`；`_build_step_context` 在下游 step 任务描述注入前序步骤文件路径 + 200 字摘要。<br><br>**主入口** `execute_stream(query, chat_history, user_id, session_id)` 被 `chat_service/agent.py` 调用；单例工厂 `get_orchestrator()`。 |
| `plan.py` | **Plan / PlanStep 数据模型 + 持久化**。<br>• `PlanStatus`：planning / executing / reflecting / completed / failed / revised / cancelled<br>• `StepStatus`：pending / ready / running / completed / failed / skipped / blocked<br>• `PlanStep.depends_on: List[int]` —— 依赖的 step_idx 列表，`get_ready_steps()` 据此返回可并行步骤<br>• CRUD：`create_plan` / `load_plan` / `update_step_status` / `replace_plan_steps` / `update_plan_status` / `get_session_plan` / `list_user_plans`<br>• `plan_id = f"plan_{毫秒时间戳}_{uuid6}"` |
| `sub_agent.py` | **SubAgent（Worker）体系**。<br>• `SubAgent` dataclass：`name` / `description` / `tools` / `system_prompt` / `workflow_hint` / `max_tool_calls=15` / `category`（builtin/domain/memory/artifact/system）<br>• `build_agent(llm)` 每次新建独立 LangChain agent 实例（线程安全），挂载 `ToolCallLimitMiddleware` / `skill_aware_monitor` / `log_before_model` / `smart_prompt_switch` 中间件<br>• `SubAgentRunner.execute_stream()` —— Worker 流式执行入口，自动注入 `runtime_context["active_skills"]`<br>• `SubAgentRegistry` —— 单例注册中心，`descriptions_for_planner()` 生成给 Planner 的菜单<br>• `run_steps_parallel(step_specs, chat_history, max_workers=5)` —— 并行执行器，`parent_ctx = contextvars.copy_context()` + 每个 worker `child_ctx = parent_ctx.copy()` 避免 "already entered" 错误 |
| `sub_agents/builtin.py` | **10 个内置 SubAgent 注册**（`register_all_subagents()` 模块加载时自动注册一次）。详见下方「内置 SubAgent 清单」 |
| `tools/agent_tools.py` | 领域工具：rag_summarize / get_weather / get_user_location / get_city_code / get_user_id / get_current_month / fetch_external_data / fill_context_for_report / get_schedule |
| `tools/file_tools.py` | `auto_fill_word` —— Word 文档 `{标签}` 占位符自动填充 |
| `tools/search_tools.py` | `search` —— 基于 Tavily Search API 的联网搜索 |
| `tools/codexec_tools.py` | **代码沙箱**：`run_python_code` / `run_shell_command` / `install_package` / `list_packages`。本地 subprocess 执行，AST 检查危险调用告警 + 命令黑名单（`rm -rf`/`format`/`shutdown` 等）+ 超时（`CODE_TIMEOUT_MAX`）+ 输出截断 5000 字。子进程 cwd = `workspace/{user_id}/YYYYMMDD/tasks/<plan_id>/`（任务级隔离） |
| `tools/browser_tools.py` | 网页抓取：完整 Chrome 请求头 + Session 保 Cookie + 随机延迟 + 自动重试；`fetch_url_rendered` 真实浏览器渲染（stealth 反检测：抹掉 `navigator.webdriver`、伪装 UA/语言/时区）；`screenshot_url` |
| `tools/memory_tools.py` | 长期记忆：`remember` / `recall` / `recall_one` / `forget` |
| `tools/artifact_tools.py` | 产物管理：`create_artifact` / `list_artifacts` / `get_artifact`，通过 `current_plan_id_var` ContextVar 关联到当前 step |
| `tools/middleware.py` | LangChain 中间件：`skill_aware_monitor`（工具监控）/ `log_before_model`（模型调用前日志）/ `smart_prompt_switch`（按上下文动态切提示词） |
| `tools/sandbox_config.py` | 沙箱配置集中管理：`SHELL_DANGEROUS_ENABLED` / `CODE_TIMEOUT_MAX` / `PYPI_MIRROR`，支持环境变量展开 |

#### 内置 SubAgent 清单（sub_agents/builtin.py）

| category | name | 能力 | 工作流 |
|----------|------|------|--------|
| domain | weather | 天气查询 | get_user_location → get_city_code → get_weather |
| domain | schedule | 课表/日程 | get_current_month → get_schedule |
| domain | report | 工作报告 | get_user_id → fill_context_for_report → fetch_external_data → create_artifact |
| domain | knowledge | 知识库检索 | rag_summarize |
| domain | document | Word 填充 | auto_fill_word |
| domain | search | 联网搜索 | search (Tavily) |
| builtin | codexec | 代码沙箱 | run_python_code / run_shell_command / install_package / list_packages |
| builtin | browser | 网页抓取 | fetch_url / fetch_url_rendered / extract_links / read_page_main_text / screenshot_url |
| memory | memory | 长期记忆 | remember / recall / recall_one / forget |
| artifact | artifact | 产物管理 | create_artifact / list_artifacts / get_artifact |

### config/ — YAML 配置（仅 AIRAGAgent 引擎使用）

| 文件 | 说明 |
|------|------|
| `agent.yml` | 沙箱配置（`shell_dangerous_enabled` / `code_timeout_max` / `pypi_mirror`），支持 `${PROJECT_ROOT}` / `~` / `${ENV_VAR}` 展开 |
| `chroma.yml` | ChromaDB 配置：集合名、持久化目录、嵌入提供者（`ollama`/`dashscope`）、分块参数 |
| `rag.yml` | DeepSeek 对话模型 + DashScope 嵌入 API Key、模型名 |
| `mysql.yml` | MySQL 连接（微服务架构下由各服务 `core/config.py` 的环境变量覆盖） |
| `redis.yml` | Redis 连接 |
| `prompts.yml` | 提示词文件路径映射 |

### database/ — 数据访问层

| 文件 | 说明 |
|------|------|
| `connection.py` | `get_db()` 上下文管理器（pymysql + DictCursor + autocommit）；`init_db()` 创建全部表（conversations/users/messages/knowledge_bases/kb_documents 等）；`mysql_conf` 字典在运行时可被微服务 patch 重定向到对应库 |
| `models.py` | 会话/消息持久化模型（被 chat_service 复用） |

### kb/ — 多知识库 RAG（被 kb_service 复用）

| 文件 | 说明 |
|------|------|
| `models.py` | `knowledge_bases` 表（scope=global/personal、biz_line、is_default 保护、created_by）+ `kb_documents` 表（filename/md5/version/status=pending/processing/ready/failed/chunk_count） |
| `service.py` | 独立 Chroma 集合 `kb_store`（与旧 `agent` 集合共存，按 `kb_id/doc_id` 元数据隔离）；文档索引（分块+嵌入+元数据）、后台线程异步索引、删除/替换/整库重建、分块预览、带相似度分数检索、默认库播种（自动收录 `data/` 目录文件，MD5 去重）；Chroma 与嵌入模型懒加载 |
| `embedding.py` | Ollama HTTP 直连嵌入（兼容 DashScope） |

### rag/ — 向量存储 + 检索

| 文件 | 说明 |
|------|------|
| `vector_store.py` | ChromaDB 封装：初始化、`RecursiveCharacterTextSplitter` 分块、PDF/TXT/Excel 多格式加载、MD5 增量更新、`get_retriever()` |
| `rag_service.py` | RAG 总结服务：检索 → 拼接上下文 → LLM 总结；KB 感知（按当前用户解析可访问库，缓存键含用户与库签名） |

### model/ — 模型工厂

| 文件 | 说明 |
|------|------|
| `factory.py` | `ChatModelFactory`（DeepSeek/通义千问）+ `EmbeddingsFactory`（DashScope）；导出全局 `chat_model` / `embed_model` 单例；两处 `ChatDeepSeek` 调用均加 `request_timeout=240, max_retries=1` 防挂起 |

### prompts/ — 提示词模板

| 文件 | 说明 |
|------|------|
| `identity_prompt.txt` | Worker 身份提示词（ReAct 流程、工具概览、输出规则） |
| `main_prompt.txt` | 完整版系统提示词（向后兼容保留） |
| `tools_prompt.txt` | 工具详细描述（入参/出参/调用规则） |
| `rag_summarize.txt` | RAG 总结提示词 |
| `report_prompt.txt` | 报告生成专用提示词 |

### utils/ — 工具函数

| 文件 | 说明 |
|------|------|
| `paths.py` | `get_user_workspace(user_id)` / `get_daily_workspace(user_id, date)` / `get_screenshot_dir(user_id)` —— 用户级工作区隔离（`workspace/{user_id}/{YYYYMMDD}/`） |
| `config_handler.py` | YAML 配置加载器（`load_rag_config` / `load_chroma_config` / `load_prompts_config` / `load_agent_config`） |
| `file_handler.py` | pdf_loader / txt_loader / excel_loader / listdir_with_allowed_type / get_file_md5_hex |
| `logger_handler.py` | coloredlogs 彩色日志，按日期生成日志文件，自动清理超过 7 天的旧日志 |
| `prompt_loader.py` | 提示词加载器（按配置从 txt 文件加载各类提示词） |
| `path_tool.py` | `get_project_root()` 及相对/绝对路径转换 |

### infrastructure/ — 基础设施

| 文件 | 说明 |
|------|------|
| `redis_client.py` | Redis 连接池 + 客户端 |
| `session_cache.py` | 会话消息缓存（Redis，缓存键含用户与会话） |
| `rag_cache.py` | RAG 检索缓存（用户与库签名失效） |
| `rate_limiter.py` | 限流器（基于 Redis 计数） |
| `task_queue.py` | 后台任务队列（plan 后台执行模式） |

### skills/ — 技能扩展框架

| 文件 | 说明 |
|------|------|
| `base.py` | 技能基类 |
| `definitions.py` | 技能定义注册 |

---

## 三、services/ — 微服务层

6 个独立 FastAPI 微服务，**每个服务自带 `core/` 库（绝对解耦，无共享包）**。配置纯环境变量驱动，启动方式：

```bash
cd services/<svc> && uvicorn main:app --app-dir . --port <PORT>
# chat_service / kb_service 需 PYTHONPATH 指向项目根（AIRAGAgent 引擎）
```

### 通用 core/ 库结构（每个服务一致）

| 文件 | 说明 |
|------|------|
| `core/config.py` | 纯环境变量配置：`PORT`/`DB_NAME`/`MYSQL_*`/`REDIS_*`/`JWT_SECRET_KEY`/`INTERNAL_HOST`/`*_PORT`/`*_SERVICE_URL`。`get_service_url(name)` 推导跨服务调用地址（优先 `*_SERVICE_URL` 显式覆盖，其次 `INTERNAL_HOST` + `*_PORT`） |
| `core/base_app.py` | `create_app(service_name, version, on_startup=None)` —— 统一 FastAPI 工厂：CORS（`*`）、`lifespan`、自动注册 `GET /api/health`（返回 status/service/redis） |
| `core/db.py` | `get_connection()` / `get_db()` 上下文管理器（pymysql + DictCursor + autocommit）/ `ensure_database_exists()` |
| `core/jwt_auth.py` | `create_access_token(user_id, username, role, device_token)` / `decode_token(token)` / `get_current_user(request, credentials)` 依赖（支持 `Authorization: Bearer` 与 `?token=` 两种方式）/ `get_admin_user`（role=admin + IP 白名单）/ `get_client_ip(request)` |
| `core/http_client.py` | `ServiceClient(name)` —— 基于 httpx.AsyncClient 的跨服务调用封装，从 `get_service_url(name)` 取目标地址 |
| `core/redis_client.py` | Redis 连接池 + 客户端 |
| `core/logger.py` | 日志配置 |
| `core/paths.py` | 路径常量（`LOG_DIR` / `UPLOAD_DIR` / `WORKSPACE_DIR`） |
| `core/events.py` | Redis Pub/Sub 频道常量（chat_service / admin_service 用） |

### auth_service/ (:8001, DB: lc_auth)

| 文件 | 说明 |
|------|------|
| `main.py` | 15 个路由：`POST /api/auth/register`、`POST /api/auth/login`、`GET /api/auth/me`、`GET /api/auth/recover/question`、`POST /api/auth/recover/reset`；`PUT /api/user/password`、`PUT /api/user/security-question`、`GET /api/user/devices`、`DELETE /api/user/devices/{id}`、`POST /api/user/devices/revoke-all-others`；内部接口 `/internal/auth/user/{id}`、`/internal/auth/users`、`POST /internal/auth/users/{id}/delete`、`POST /internal/auth/users/{id}/reset-password`、`GET /internal/auth/users/{id}/role` |
| `models.py` | `users` 表（id/username/password_hash/role/created_at/last_password_changed）、`security_questions` 表、`login_devices` 表（device_token/device_type/os/browser/ip/is_revoked，支持多端登录管理） |

### user_service/ (:8002, DB: lc_user)

| 文件 | 说明 |
|------|------|
| `main.py` | 用户档案、会话元数据、消息反馈、会话分叉/搜索；内部接口供 admin_service 调用 |
| `models.py` | 用户档案、会话、消息、反馈数据模型 |

### chat_service/ (:8003, DB: lc_chat)

| 文件 | 说明 |
|------|------|
| `main.py` | **核心入口**。`POST /api/chat` —— JWT 鉴权 + IP 提取 + ContextVar 注入（user_id/ip/lat/lon）+ 限流（超限发 Redis `CHANNEL_RATE_LIMITED` 事件）+ SSE `StreamingResponse`（`X-Accel-Buffering: no`）。`POST /api/plan` —— DeepAgent plan 入口（stream 或 background）。`GET /api/plans` / `GET /api/plans/{plan_id}` / `GET /api/subagents`（前端计划历史 / Agent 能力卡片）。会话 CRUD `/api/conversations`、`/api/conversations/search`、`/api/conversations/{id}/branch`。反馈 `/api/feedback`。内部接口 `/internal/chat/users/{id}/conversations`、`DELETE /internal/chat/users/{id}/cleanup`（供 user_service 注销账号清理） |
| `agent.py` | `AgentService` 封装 Orchestrator 单例。`stream_response(user_id, message, session_id, truncate_to)` —— 用 `asyncio.Queue` + `contextvars.copy_context()` 把同步生成器 `Orchestrator.execute_stream` 桥接到异步流；`sync_producer` 在 `loop.run_in_executor` + `ctx.run` 中跑：取会话历史（先查 Redis 缓存，未命中查 DB）→ 流式 → 落库 user/assistant 消息 + emit `message_ids`。finally 埋点：`log_api_call` 写 `lc_chat` 库 + `publish_event(CHANNEL_CHAT_COMPLETED, ...)` 发 Redis 事件供 admin_service 聚合。`enqueue_plan_background` 投递到 `task_queue` 后台执行 |
| `db_patch.py` | `apply_db_redirect()` —— 必须在 import AIRAGAgent 之前完成，把 AIRAGAgent 的 DB 指向 `lc_chat` 库 |

### kb_service/ (:8004, DB: lc_kb)

| 文件 | 说明 |
|------|------|
| `main.py` | 28 个路由。**管理端** `/api/admin/kb/*`：list/create/patch/delete、documents CRUD（上传/批量删除/替换/重索引）、chunks 预览、rebuild 重建、search-test 检索测试、embedding-status 嵌入诊断。**用户端** `/api/kb/*`：mine 列表/创建/更新/删除、documents CRUD（个人库归属校验）、search-test。**内部** `/internal/kb/search`（无鉴权，按 user_id 解析可访问库）。启动钩子 `_on_startup` 调 `seed_default_kb()` 播种默认知识库。模块加载时 patch `AIRAGAgent.database.connection.mysql_conf["database"] = DB_NAME` 重定向 kb_models 到 `lc_kb` |

### admin_service/ (:8005, DB: lc_admin)

| 文件 | 说明 |
|------|------|
| `main.py` | 统计/审计/限流管理。订阅 Redis Pub/Sub 频道（`CHANNEL_CHAT_COMPLETED` / `CHANNEL_RATE_LIMITED`）聚合事件落库；调 auth/user/chat 服务做用户/会话管理。`core/events.py` 提供 Redis 订阅器 |
| `models.py` | API 调用日志、限流记录、统计聚合表 |

### file_service/ (:8006, 无 DB)

| 文件 | 说明 |
|------|------|
| `main.py` | `/api/file/list`、`/api/file/download`、`/api/file/preview` —— JWT 鉴权（`Depends(get_current_user)`）按 `user["id"]` 限定根目录到 `workspace/{user_id}/`。`.playwright` 隐藏目录前端文件列表过滤。无 DB（文件系统即存储） |

### 微服务协作图

```
[前端] ──nginx──→ chat_service(8003) ──→ Orchestrator + SubAgents
                    │                        ↓
                    │                   user_service(8002)  (AIRAGAgent 引擎直连)
                    │
                    ├── Redis Pub/Sub ──→ admin_service(8005) 聚合
                    │
                    └── MySQL: lc_chat 库（plans / plan_steps / conversations / messages）

auth_service(8001)  ←── user_service / admin_service 调用
kb_service(8004)    独立（MySQL lc_kb + ChromaDB kb_store）
file_service(8006)  独立（文件系统存储）
```

---

## 四、frontend/ — Vue 3 前端

| 目录/文件 | 说明 |
|-----------|------|
| `vite.config.js` | Vite 配置：`@` 路径别名、未设 `VITE_API_TARGET` 时按 URL 前缀代理到 8001-8006、`manualChunks` 拆 `vue-vendor` 独立 chunk、构建输出到 `../static` |
| `src/api/auth.js` `chat.js` `kb.js` `file.js` | API 客户端（chat.js 处理 SSE 流式响应；file.js 带 token 兼容） |
| `src/views/ChatView.vue` | 聊天主视图，挂载 MessageBubble / ChatInput / WelcomeScreen / SideDrawer / PlanPanel / FileBrowser |
| `src/views/AdminView.vue` `AdminKbView.vue` `UserKbView.vue` `SettingsView.vue` `Login.vue` | 各页面视图 |
| `src/composables/useChat.js` | 聊天状态管理：消息列表、会话、流式输出状态、思考过程、对话历史加载/保存/删除、流式未完成关闭网页防丢失（push userMsg 后立即 fire-and-forget 保存会话） |
| `src/composables/useTheme.js` | 主题切换 |
| `src/components/MessageBubble.vue` | 消息气泡 + markdown-it 渲染（50ms 流式节流） |
| `src/components/PlanPanel.vue` | 计划执行可观测面板（step 状态、思考、反思） |
| `src/components/PlanHistory.vue` `AgentCapabilities.vue` | 计划历史 + Agent 能力清单抽屉（按需加载 step 明细） |
| `src/components/kb/KbManager.vue` | 知识库管理（拖拽批量上传、文档表格、分块预览、检索测试 playground）—— `defineAsyncComponent` 懒加载 |
| `src/components/FileBrowser.vue` | AI 工作区文件浏览器抽屉 —— `defineAsyncComponent` 懒加载 |
| `src/components/SettingsPanel.vue` | 设置面板 —— `defineAsyncComponent` 懒加载 |
| `src/components/SideDrawer.vue` | 侧边抽屉容器（计划历史 / Agent 能力 / 文件浏览器） |
| `src/components/LineChart.vue` `StackedAreaChart.vue` | admin 统计图表 |
| `src/utils/markdown.js` | markdown-it 渲染工具 |
| `src/router/index.js` | Vue Router（`beforeEach` 守卫调 `/api/auth/me` 校验 token） |
| `src/styles/global.css` | 全局样式（主界面深色文本 rgba(20,20,25,0.95)，预览弹窗深色蒙层保留白色文本） |

---

## 五、其他目录

| 目录 | 说明 |
|------|------|
| `deploy/` | 部署脚本（`deploy_to_server.py` paramiko SFTP + systemd 重启）+ nginx 配置 + `.env` 模板。**临时调试脚本（`_*.py` / `check_*.py` / `fix_*.py` / `verify_*.py` 等）已清理，仅保留可复用部署工具** |
| `deploy/backend/` | `.env.example` / `.env.microservices.example` / `start_microservices.bat` / `start_microservices.sh` / `stop_microservices.sh` |
| `deploy/frontend/nginx.conf` | 生产 nginx 配置（gzip/brotli 压缩、`dist/assets/*` immutable 缓存、`index.html` no-cache、按 URL 前缀代理微服务） |
| `uploads/` | 用户上传文件：`avatars/`（头像）、`kb/{kb_id}/{md5}.{ext}`（知识库文档，内容寻址去重）、`screenshots/`（截图）。**已加入 .gitignore** |
| `workspace/` | AI 工作区：`{user_id}/{YYYYMMDD}/tasks/<plan_id>/`（用户 → 日期 → 计划三级隔离）；`.playwright/` 存储 Chromium 二进制。**已加入 .gitignore** |
| `chroma_ab/` | ChromaDB 持久化目录（`chroma.sqlite3` + 二进制向量文件）。**已加入 .gitignore** |
| `tests/` | pytest 测试（`auth_service/` / `kb_service/`），独立测试库 `lc_auth_test` / `lc_kb_test`，session 自动建表、每测自动清理 |
| `AIRAGAgent/logs/` `services/*/logs/` | 运行时日志（按日期 + 服务）。**已加入 .gitignore** |

---

## 六、架构总结

```
用户浏览器 ──→ nginx ──→ chat_service(8003) ──→ Orchestrator.execute_stream()
                                  │                    │
                                  │                    ├─ 1. Planner 规划
                                  │                    │     • 召回长期记忆
                                  │                    │     • 解析 JSON → plans/plan_steps 表
                                  │                    │
                                  │                    ├─ 2. Executor + Reflector 循环
                                  │                    │     • get_ready_steps() 依赖感知
                                  │                    │     • 单步串行 / 多步并行 (ThreadPool max=5)
                                  │                    │     • Reflector: accept/retry/revise/ask_user
                                  │                    │     • 每步结果落盘 + 下游注入路径摘要
                                  │                    │
                                  │                    └─ 3. Finalizer 汇总
                                  │                          • 单步复用 / 多步 LLM 整合
                                  │
                                  ├─ Redis Pub/Sub ──→ admin_service(8005) 聚合
                                  └─ MySQL: lc_chat 库

auth_service(8001)  ←── user_service / admin_service
kb_service(8004)    独立（lc_kb + ChromaDB kb_store）
file_service(8006)  独立（文件系统存储，按 user_id 隔离）
```

**核心流程**：用户输入 → Planner 规划（流式思考）→ Executor 执行（SubAgent 并行）→ Reflector 反思（accept/retry/revise/ask_user）→ Finalizer 汇总 → SSE 流式输出。全程 `plan_created / step_* / plan_reflecting / plan_revised / plan_completed` 事件可观测。
