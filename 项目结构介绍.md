# lc-course 项目结构介绍

## 项目概述

本项目是一个基于 **LangChain + LangGraph** 的**多智能体协同自动化办公助手**，采用 **Supervisor-Worker 多智能体架构**。前端使用 **Vue 3 + Vite** 构建，后端同时提供 **Streamlit** 和 **FastAPI** 两种服务入口，集成 **RAG（检索增强生成）** 知识库、向量数据库、联网搜索、微信消息推送、Word 文档自动填充、课表查询等多种办公自动化能力。

---

## 项目根目录

| 文件/目录 | 说明 |
|-----------|------|
| `pyproject.toml` | Python 项目配置文件，定义了项目名称 `lc-course`、Python 版本要求（≥3.13）及所有依赖（LangChain、ChromaDB、FastAPI、Streamlit 等） |
| `uv.lock` | UV 包管理器的依赖锁定文件，确保环境一致性 |
| `langgraph.json` | LangGraph 配置文件，定义了图的入口点 `chief_agent`（指向 `react_agent.py:graph`）和环境变量文件 `.env` |
| `.gitignore` | Git 忽略规则文件 |

---

## AIRAGAgent/ — 核心 Python 后端包

项目主模块，包含智能体、RAG、模型工厂、配置、工具等所有核心逻辑。

### 根级文件

| 文件 | 说明 |
|------|------|
| `__init__.py` | 包初始化文件（空文件，使目录成为 Python 包） |
| `app.py` | **Streamlit 前端入口**，提供浏览器端的聊天界面，加载 SupervisorAgent 多智能体系统，支持流式输出思考和回答内容 |
| `initialize_vector_db.py` | **向量数据库初始化脚本**，将 `data/` 目录下的知识文件加载到 ChromaDB 向量存储中，并执行测试检索验证 |
| `md5.txt` | 存储已加载文件的 MD5 哈希值，用于增量更新向量数据库时判断文件是否发生变化 |

---

### agent/ — 智能体模块

实现多智能体协同架构（Supervisor-Worker 模式）。

| 文件 | 说明 |
|------|------|
| `react_agent.py` | **Worker 工作智能体**，基于 LangGraph 的 ReAct（推理-行动）模式，挂载了 RAG 检索、天气查询、用户定位、课表查询、Word 填充、联网搜索、微信发送等全部工具，使用本地模型（Ollama）运行 |
| `supervisor_agent.py` | **Supervisor 监督智能体**，负责分析用户需求、将任务委派给 Worker 智能体、审查执行结果。支持 ACCEPT/RETRY 决策机制，确保输出质量符合用户需求 |

#### agent/tools/ — 智能体工具集

| 文件 | 说明 |
|------|------|
| `agent_tools.py` | **核心工具集合**，包含以下工具函数：<br>• `rag_summarize` — 从向量知识库检索并总结相关内容<br>• `get_weather` — 通过高德天气 API 获取城市天气预报<br>• `get_user_location` — 通过 IP 定位获取用户当前省市<br>• `get_city_code` — 城市名称转高德城市编码（adcode）<br>• `get_user_id` — 获取模拟用户 ID<br>• `get_current_month` — 获取指定日期的日期、星期、学期周次<br>• `fetch_external_data` — 从外部 CSV 数据获取用户工作记录<br>• `fill_context_for_report` — 报告生成场景的上下文标记<br>• `get_schedule` — 从课表 Excel 查询指定周次和星期的课程 |
| `file_tools.py` | **文件操作工具**，提供 `auto_fill_word` 工具，自动识别 Word 文档中的 `{标签}` 占位符并通过 AI 生成内容进行填充 |
| `search_tools.py` | **联网搜索工具**，基于 Tavily Search API，提供 `search` 工具用于互联网实时信息检索 |
| `wx_tools.py` | **微信消息工具**，通过 `uiautomation` 库操控微信桌面客户端，实现 `send_wx_message` 自动向指定联系人发送消息 |
| `middleware.py` | **中间件模块**，包含三个 LangChain 中间件：<br>• `monitor_tool` — 工具调用监控，记录工具执行日志和异常<br>• `log_before_model` — 模型调用前日志记录<br>• `smart_prompt_switch` — 根据上下文动态切换系统提示词（如报告生成时切换到专用提示词） |

---

### config/ — 配置文件

| 文件 | 说明 |
|------|------|
| `rag.yml` | **RAG 和模型配置**，包含：<br>• 远程模型名（deepseek-v4-flash）和 API Key<br>• 嵌入模型名（text-embedding-v4）<br>• 高德地图 API Key<br>• 本地模型名（qwen3:4b）和本地嵌入模型（nomic-embed-text） |
| `chroma.yml` | **ChromaDB 向量数据库配置**，包含集合名、持久化目录、检索数量 k、文档分块大小/重叠量、分隔符、允许的知识文件类型等 |
| `prompts.yml` | **提示词文件路径配置**，映射各提示词的 txt 文件路径（main_prompt、identity_prompt、tools_prompt、rag_summarize、report_prompt） |
| `agent.yml` | **智能体配置**，目前配置了外部数据文件路径 `data/external/records.csv` |

---

### data/ — 数据文件

| 文件/目录 | 说明 |
|-----------|------|
| `external/records.csv` | **外部办公数据记录**，包含各用户的特征（户型、家庭结构、地面类型）、清洁效率、耗材情况、对比数据等，供报告生成功能使用 |
| `24级土木1班课表-2025-2026-2.xlsx` | **学期课表文件**，供 `get_schedule` 工具查询指定周次和星期的课程安排 |

---

### fastapi_app/ — FastAPI Web 服务

提供 RESTful API 接口，同时挂载 Vue 前端静态文件。

| 文件 | 说明 |
|------|------|
| `__init__.py` | 包初始化文件（空） |
| `main.py` | **FastAPI 应用入口**，完成以下工作：<br>• 定义 `/api/chat` 等 API 路由<br>• 挂载 `static/` 目录为静态文件服务<br>• 配置 CORS 中间件<br>• 启动时预热本地 Ollama 模型（聊天模型 + 嵌入模型）<br>• `lifespan` 生命周期管理 |

#### fastapi_app/services/ — 服务层

| 文件 | 说明 |
|------|------|
| `agent_service.py` | **Agent 服务类**，封装 SupervisorAgent 的调用逻辑，提供异步流式/非流式接口，管理会话存储 |
| `rag_service.py` | **RAG 服务类**，封装向量知识库检索功能，提供异步搜索接口，返回文档内容和元数据 |

---

### logs/ — 日志目录

| 文件 | 说明 |
|------|------|
| `agent_YYYYMMDD.log` | 按日期生成的运行日志文件，记录智能体执行过程、工具调用、错误信息等。系统自动清理超过 7 天的旧日志 |

---

### model/ — 模型工厂模块

使用工厂模式统一管理本地和远程 LLM 模型。

| 文件 | 说明 |
|------|------|
| `__init__.py` | 包初始化文件（空） |
| `factory.py` | **远程模型工厂**，包含：<br>• `ChatModelFactory` — 根据配置创建 DeepSeek 或通义千问远程聊天模型<br>• `EmbeddingsFactory` — 创建 DashScope 嵌入模型<br>• 导出全局 `chat_model` 和 `embed_model` 单例 |
| `local_factory.py` | **本地模型工厂**，基于 Ollama 创建：<br>• `ChatModelFactory` — 创建本地聊天模型（默认 qwen3:4b）<br>• `EmbeddingsFactory` — 创建本地嵌入模型（默认 nomic-embed-text）<br>• 导出全局 `local_chat_model` 和 `local_embed_model` 单例 |

---

### prompts/ — 提示词模板

| 文件 | 说明 |
|------|------|
| `identity_prompt.txt` | **身份提示词**（精简版），定义助手角色、核心思考准则（ReAct 流程）、工具概览和输出规则。Worker 智能体使用此提示词 |
| `main_prompt.txt` | **完整版系统提示词**，包含更详细的工具使用说明（向后兼容保留） |
| `tools_prompt.txt` | **工具详细描述**，包含每个工具的入参说明、出参格式、使用场景和调用规则（如天气查询的三步强制流程） |
| `rag_summarize.txt` | **RAG 总结提示词**，用于参考文档资料总结回复时的提示词模板 |
| `report_prompt.txt` | **报告生成专用提示词**，当智能体触发报告生成场景时动态切换使用 |

---

### rag/ — RAG（检索增强生成）模块

| 文件 | 说明 |
|------|------|
| `rag_service.py` | **RAG 总结服务**，核心流程：<br>① 接收用户问题 → ② 从向量数据库检索相关文档 → ③ 将文档拼接为上下文 → ④ 提交给 LLM 进行总结回复 |
| `vector_store.py` | **向量数据库服务**，封装 ChromaDB 操作：<br>• 初始化向量存储（指定集合名、嵌入模型、持久化目录）<br>• 使用 `RecursiveCharacterTextSplitter` 进行文档分块<br>• 支持 PDF、TXT、Excel 多种格式的文档加载<br>• 基于文件 MD5 实现增量更新（避免重复加载）<br>• 提供 `get_retriever()` 获取检索器实例 |

---

### utils/ — 工具函数模块

| 文件 | 说明 |
|------|------|
| `__init__.py` | 包初始化文件（空） |
| `config_handler.py` | **配置加载器**，从 YAML 配置文件加载：<br>• `load_rag_config` — RAG 配置<br>• `load_chroma_config` — ChromaDB 配置<br>• `load_prompts_config` — 提示词路径配置<br>• `load_agent_config` — 智能体配置<br>模块加载时自动读取并导出全局配置变量 |
| `file_handler.py` | **文件处理器**，提供：<br>• `pdf_loader` — PDF 文档加载（PyPDF）<br>• `txt_loader` — TXT 文档加载<br>• `excel_loader` — Excel 文档加载（pandas）<br>• `listdir_with_allowed_type` — 按后缀过滤文件列表<br>• `get_file_md5_hex` — 计算文件 MD5 用于增量更新判断 |
| `logger_handler.py` | **日志处理器**，配置 `coloredlogs` 彩色日志输出，支持按日期生成日志文件，自动清理超过 7 天的旧日志 |
| `path_tool.py` | **路径工具**，提供 `get_project_root()` 获取项目根目录绝对路径，以及基于根目录的相对路径转绝对路径功能 |
| `prompt_loader.py` | **提示词加载器**，从配置指定的 txt 文件加载各类提示词：<br>• `load_system_prompts` — 完整版系统提示词<br>• `load_identity_prompts` — 身份提示词<br>• `load_rag_prompts` — RAG 总结提示词<br>• `load_report_prompts` — 报告生成提示词<br>• `load_tool_details` — 工具详细描述（按需加载） |

---

## chroma_ab/ — 向量数据库持久化目录

ChromaDB 的持久化存储目录，包含向量数据和元数据的 SQLite 数据库文件。

| 目录/文件 | 说明 |
|-----------|------|
| `chroma.sqlite3` | ChromaDB 的 SQLite 元数据数据库 |
| `<uuid>/` | 向量数据的二进制存储文件（data_level0.bin, header.bin, length.bin, link_lists.bin） |

---

## frontend/ — Vue.js 前端项目

基于 Vue 3 + Vite 构建的聊天交互界面。

### 根级文件

| 文件 | 说明 |
|------|------|
| `index.html` | 前端入口 HTML 文件 |
| `package.json` | Node.js 项目配置，依赖 Vue 3 和 Vite，包含 dev/build/preview 脚本 |
| `package-lock.json` | npm 依赖锁定文件 |
| `vite.config.js` | Vite 构建配置：配置 `@` 路径别名、API 代理到 `http://127.0.0.1:8000`、构建输出到 `../static` 目录 |
| `.gitignore` | 前端 Git 忽略规则 |

### public/ — 静态资源

| 文件 | 说明 |
|------|------|
| `favicon.svg` | 网站图标 |
| `icons.svg` | SVG 图标集 |

### src/ — 源代码

| 文件 | 说明 |
|------|------|
| `main.js` | Vue 应用入口，创建 App 实例并挂载，导入全局样式 |
| `App.vue` | 根级 Vue 组件，组合 ChatInput、MessageBubble、WelcomeScreen 等子组件 |

#### src/api/ — API 通信层

| 文件 | 说明 |
|------|------|
| `chat.js` | **聊天 API 客户端**，封装所有后端通信：<br>• `sendChatMessage` — 发送消息并处理 SSE 流式响应<br>• `generateSessionId` — 生成唯一会话 ID<br>• `loadConversationsApi` — 加载历史对话列表<br>• `saveConversationApi` — 保存对话<br>• `deleteConversationApi` — 删除对话 |

#### src/components/ — Vue 组件

| 文件 | 说明 |
|------|------|
| `ChatInput.vue` | 聊天输入框组件，处理用户消息输入和发送 |
| `ErrorBanner.vue` | 错误提示横幅组件 |
| `MessageBubble.vue` | 消息气泡组件，渲染用户和 AI 的消息内容 |
| `WelcomeScreen.vue` | 欢迎屏组件，在无对话时显示引导信息 |

#### src/composables/ — Vue 组合式函数

| 文件 | 说明 |
|------|------|
| `useChat.js` | **聊天状态管理组合式函数**，管理消息列表、会话 ID、流式输出状态、思考过程展示、对话历史加载/保存/删除等核心逻辑 |

#### src/styles/ — 样式

| 文件 | 说明 |
|------|------|
| `global.css` | 全局 CSS 样式文件 |

#### src/utils/ — 前端工具

| 文件 | 说明 |
|------|------|
| `markdown.js` | Markdown 渲染工具，将 AI 返回的 Markdown 格式文本转换为 HTML |

---

## static/ — 前端构建产物

由 `vite build` 生成的静态文件，FastAPI 直接挂载此目录作为前端页面。

| 文件/目录 | 说明 |
|-----------|------|
| `index.html` | 构建后的入口 HTML |
| `favicon.svg` | 网站图标 |
| `icons.svg` | SVG 图标集 |
| `assets/` | 构建后的 JS 和 CSS 资源文件（带内容哈希命名） |

---

## 架构总结

```
用户浏览器
    │
    ├─── 方式一: Streamlit ──→ app.py → SupervisorAgent → ReactAgent (Worker)
    │
    └─── 方式二: Vue 前端 ──→ FastAPI (main.py) → AgentService → SupervisorAgent
                                                              │
                                                    ReactAgent (Worker)
                                                      │
                                         ┌────────────┼────────────┐
                                         │            │            │
                                     工具调用       RAG检索     模型推理
                                    (12个工具)   (ChromaDB)   (Ollama/远程)
```

**核心流程**：用户输入 → Supervisor 分析需求 → 委派 Worker 执行 → Worker 调用工具 + RAG 检索 → 返回结果 → Supervisor 审查 → 输出最终回答
