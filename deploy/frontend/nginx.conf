# ====== 前端 Nginx 配置 ======
# 将此文件复制到 nginx 配置目录，根据实际情况修改域名和路径

server {
    listen 80;
    server_name your-domain.com;   # 修改为你的域名或服务器 IP

    # 前端静态文件根目录
    root /var/www/lc-course-frontend;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
    gzip_min_length 1024;

    # SPA 路由 - 所有非 API 请求回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # API 反向代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000;          # 修改为后端地址
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 流式支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;

        # 禁止缓冲
        chunked_transfer_encoding on;
    }
}

# HTTPS 配置（使用 certbot 自动获取证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#
#     root /var/www/lc-course-frontend;
#     index index.html;
#
#     location / { ... }
#     location /api/ { ... }
# }
